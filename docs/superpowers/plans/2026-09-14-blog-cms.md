# Blog CMS Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Give the admin a real CRUD screen for blog posts and categories (backed by Supabase),
replacing the 6 hardcoded posts currently baked into the frontend, and give each post a real
public URL.

**Architecture:** Two new tables (`blog_categories`, `blog_posts`) in `simDulichNew`. Public reads
(published posts only) need no auth, mirroring the existing public Catalog API. Admin writes reuse
the existing `authenticate`/`requireRole`/`is_admin_or_staff()` pattern — no RPC needed, since
admin already gets full `select`/`insert`/`update`/`delete` via a normal `for all` RLS policy (the
INSERT-vs-SELECT RLS gap from the Order/Payment API doesn't apply here: there's no public-write
case, only public-read). Admin UI is 3 new pages in `Simdulich`, reusing the exact list/form
patterns already established by `CategoriesPage.jsx`. A TipTap rich-text editor produces the
`content` HTML string admin never has to hand-write.

**Tech Stack:** Next.js 16.3.4 Route Handlers, `@supabase/supabase-js`, Vitest (backend); React +
Vite, `@tiptap/react` + `@tiptap/starter-kit`, Vitest + Testing Library (frontend,
`D:\SimDuLich\Simdulich`).

## Global Constraints

- No "doubled path" replication — this is a brand-new module, not a port of the old Java backend.
  Paths are `/api/blog/...` (public) and `/api/blog/admin/...` (admin).
- Public endpoints (`GET /api/blog/posts`, `GET /api/blog/posts/{slug}`, `GET /api/blog/categories`)
  require no authentication and never expose `draft` posts.
- Admin endpoints require `authenticate` + `requireRole(user, ['admin', 'staff'])` — the same
  two-role gate as every other admin route in this project.
- Every error response is `{ message: "..." }` with an appropriate status code.
- `blog_posts.status` is exactly `'draft'` / `'published'` — no third state.
- Admin list endpoint (`GET /api/blog/admin/posts`) uses 0-indexed pagination, field `number` —
  matches every other admin list endpoint in this project. The public list endpoint has no
  pagination (returns the full array of published posts) — these are deliberately different
  conventions, matching this project's existing public-vs-admin pagination split; do not
  "normalize" them.
- No image upload — `image_url` is a plain string column, admin pastes a URL, exactly like
  `categories.image_url` already works.
- Frontend package additions: `@tiptap/react`, `@tiptap/pm`, `@tiptap/starter-kit` (install via
  `npm install`, do not hand-edit `package.json`'s version numbers).
- Vitest (both repos): chainable Supabase query-builder mocks in the backend (each test file
  defines its own local mock); Testing Library render/click/assert in the frontend, mocking the API
  module (`vi.mock('../../../api/blogApi')` style, matching `CategoriesPage.test.jsx`).
- Next.js 16: Route Handler `params` is a `Promise` — always `await params`.

---

### Task 1: Migration — schema, RLS, seed data

**Files:**
- Create: `supabase/migrations/0010_blog.sql`

**Interfaces:**
- Produces: tables `public.blog_categories`, `public.blog_posts`; 3 seed category rows (`guides`,
  `travel`, `compare`); 6 seed post rows. All later tasks depend on these existing.

Pure SQL, no automated test — matches the precedent of every prior schema-only migration in this
project (e.g. `0005_physical_sim_inventory.sql`, `0007_orders_payments.sql`). Verified by manual
read now, applied and manually verified against the real Supabase project in Task 8.

- [ ] **Step 1: Create `supabase/migrations/0010_blog.sql`**

```sql
create table public.blog_categories (
  id bigserial primary key,
  name text not null,
  slug text not null unique,
  created_at timestamptz not null default now()
);

create table public.blog_posts (
  id bigserial primary key,
  category_id bigint not null references public.blog_categories(id),
  title text not null,
  slug text not null unique,
  excerpt text not null,
  content text not null,
  image_url text,
  author text not null,
  status text not null default 'draft' check (status in ('draft', 'published')),
  published_at timestamptz,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create index idx_blog_posts_status_published_at on public.blog_posts(status, published_at desc);
create index idx_blog_posts_category on public.blog_posts(category_id);

alter table public.blog_categories enable row level security;
alter table public.blog_posts enable row level security;

create policy "Anyone can view blog categories"
  on public.blog_categories for select
  using (true);

create policy "Admins can manage blog categories"
  on public.blog_categories for all
  using (public.is_admin_or_staff())
  with check (public.is_admin_or_staff());

create policy "Anyone can view published blog posts"
  on public.blog_posts for select
  using (status = 'published');

create policy "Admins can manage blog posts"
  on public.blog_posts for all
  using (public.is_admin_or_staff())
  with check (public.is_admin_or_staff());

-- Seed: 3 categories carried over from the old hardcoded frontend data
-- (src/data/defaultSiteContent.js), plus the 6 posts that were baked into that file, so the
-- public site keeps showing the same content once BlogPage.jsx/Blog.jsx switch to the real API
-- (Task 7).
insert into public.blog_categories (name, slug) values
  ('Hướng dẫn cài đặt', 'guides'),
  ('Kinh nghiệm du lịch', 'travel'),
  ('So sánh & Tư vấn', 'compare');

insert into public.blog_posts (category_id, title, slug, excerpt, content, image_url, author, status, published_at) values
(
  (select id from public.blog_categories where slug = 'guides'),
  'Cách cài đặt và kích hoạt eSIM nhanh nhất trên iPhone & Android',
  'cach-cai-dat-kich-hoat-esim-iphone-android',
  'Hướng dẫn từng bước chi tiết quét mã QR và cấu hình dữ liệu di động trước khi khởi hành chuyến đi của bạn.',
  '<p class="text-base md:text-lg leading-relaxed text-slate-600 font-medium">Kích hoạt eSIM (SIM điện tử) là giải pháp vô cùng nhanh chóng để bạn chuẩn bị kết nối mạng di động nước ngoài mà không cần khay SIM vật lý. Dưới đây là hướng dẫn chuẩn xác nhất cho cả iOS và Android.</p>
<div class="bg-amber-50 border border-amber-200 rounded-2xl p-5 my-6 flex items-start gap-3">
  <div>
    <p class="font-bold text-amber-900 text-sm">Lưu ý cực kỳ quan trọng trước khi cài đặt</p>
    <ul class="list-disc list-inside text-amber-800 text-xs mt-2 space-y-1">
      <li>Thiết bị của bạn phải kết nối mạng Wi-Fi ổn định trong suốt quá trình kích hoạt.</li>
      <li>Không được tắt hoặc thoát màn hình khi quá trình tải cấu hình eSIM đang diễn ra.</li>
      <li>Mỗi mã QR chỉ quét được 1 LẦN DUY NHẤT. Tuyệt đối không tự ý xóa eSIM khi gặp sự cố mà hãy liên hệ kỹ thuật để được hỗ trợ.</li>
    </ul>
  </div>
</div>
<h2 class="text-2xl font-bold text-slate-800 mt-8">Phần 1: Cài đặt eSIM trên hệ điều hành iOS (iPhone)</h2>
<p class="text-slate-600 leading-relaxed">iPhone hỗ trợ quản lý nhiều eSIM vô cùng mượt mà. Hãy thực hiện theo các bước sau từ trước khi lên máy bay:</p>
<ol class="list-decimal list-inside space-y-3 pl-2 text-slate-700 font-semibold text-sm">
  <li>Mở ứng dụng <span class="text-primary">Cài đặt (Settings)</span> và chọn <span class="text-primary">Di động (Cellular)</span>.</li>
  <li>Bấm chọn <span class="text-primary">Thêm eSIM (Add eSIM)</span> hoặc <span class="text-primary">Thêm gói cước di động</span>.</li>
  <li>Chọn <span class="text-primary">Sử dụng mã QR (Use QR Code)</span>.</li>
  <li>Đưa camera điện thoại quét mã QR được gửi trong Email đơn hàng của bạn.</li>
  <li>Chờ 30 giây để thiết bị tải cấu hình. Sau khi hoàn tất, hãy đặt tên nhãn cho eSIM mới này (Ví dụ: "SIMDULICH.VN Du Lịch").</li>
</ol>
<h2 class="text-2xl font-bold text-slate-800 mt-8">Phần 2: Cài đặt eSIM trên hệ điều hành Android (Samsung, Pixel)</h2>
<ol class="list-decimal list-inside space-y-3 pl-2 text-slate-700 font-semibold text-sm">
  <li>Truy cập <span class="text-primary">Cài đặt (Settings)</span> → chọn <span class="text-primary">Kết nối (Connections)</span>.</li>
  <li>Chọn mục <span class="text-primary">Quản lý SIM (SIM Manager)</span>.</li>
  <li>Bấm chọn <span class="text-primary">Thêm eSIM (Add mobile plan)</span>.</li>
  <li>Chọn <span class="text-primary">Quét mã QR từ nhà mạng (Scan carrier QR code)</span>.</li>
  <li>Quét mã QR từ email của bạn và bấm <span class="text-primary">Thêm (Add / Confirm)</span> để hoàn thành.</li>
</ol>
<div class="bg-[#F9F5FD] rounded-2xl p-6 border border-[#F0E5FA] mt-8">
  <h3 class="font-bold text-slate-800 text-base mb-2">Cách kích hoạt sử dụng khi đáp xuống sân bay quốc tế</h3>
  <p class="text-slate-600 text-sm leading-relaxed">Ngay khi máy bay hạ cánh: Vào <strong>Cài đặt di động</strong> → Bật đường truyền <strong>SIMDULICH.VN</strong> → Chọn dòng này làm <strong>Dữ liệu di động chính (Cellular Data)</strong> → Đồng thời bật nút <strong>Chuyển vùng dữ liệu (Data Roaming)</strong> của eSIM lên để kết nối internet quốc tế.</p>
</div>',
  '/images/blog_image_1.png',
  'Kỹ thuật viên SIMDULICH.VN',
  'published',
  '2026-06-17 00:00:00+07'
),
(
  (select id from public.blog_categories where slug = 'travel'),
  'Kinh nghiệm du lịch tự túc Thái Lan với kết nối mạng 5G không giới hạn',
  'kinh-nghiem-du-lich-thai-lan-esim-5g',
  'Chia sẻ mẹo chọn gói cước eSIM và giữ liên lạc thông suốt trong suốt hành trình phượt Bangkok - Phuket.',
  '<p class="text-base md:text-lg leading-relaxed text-slate-600 font-medium">Du lịch Thái Lan tự túc đã trở thành lựa chọn quen thuộc của đông đảo bạn trẻ Việt. Tuy nhiên, để có chuyến đi trọn vẹn thì internet tốc độ cao là điều bắt buộc phải có để dò đường Google Maps, đặt xe Grab/Bolt và dịch thuật tại chỗ.</p>
<h2 class="text-2xl font-bold text-slate-800 mt-8">Lý do vì sao bạn nên chọn eSIM thay vì mua SIM tại sân bay Bangkok</h2>
<p class="text-slate-600 leading-relaxed">Nhiều du khách thường chọn mua SIM tại quầy dịch vụ ở sân bay Suvarnabhumi hoặc Don Mueang. Tuy nhiên, phương án này có nhiều bất cập: phải xếp hàng dài sau chuyến bay mệt mỏi, chi phí đắt hơn và nguy cơ thất lạc SIM gốc của Việt Nam khi tháo lắp thiết bị.</p>
<h2 class="text-2xl font-bold text-slate-800 mt-8">Trải nghiệm tốc độ 5G của nhà mạng DTAC & AIS</h2>
<p class="text-slate-600 leading-relaxed">SIMDULICH.VN cung cấp gói cước tích hợp roaming với nhà mạng lớn nhất Thái Lan. Trải nghiệm thực tế tại Bangkok cho thấy sóng luôn căng tràn 5 vạch 5G, tốc độ download lên đến 180 Mbps, cho phép livestream HD và thực hiện cuộc gọi video hoàn toàn không bị trễ/giật.</p>
<h3 class="text-lg font-bold text-slate-800 mt-6">Lịch trình đề xuất & Gói cước khuyên dùng:</h3>
<ul class="list-disc list-inside space-y-2 text-slate-600 text-sm">
  <li>Lịch trình 3-5 ngày (Bangkok & Pattaya): Chọn gói eSIM 1GB/Ngày tốc độ cao là đủ dùng cho nhu cầu cơ bản.</li>
  <li>Lịch trình trên 5 ngày hoặc có đi Phuket/Chiang Mai: Khuyên dùng gói dữ liệu Không giới hạn dung lượng để phát sóng thoải mái cho bạn bè đi cùng.</li>
</ul>',
  '/images/blog_image_2.png',
  'Admin SIMDULICH.VN',
  'published',
  '2026-06-16 00:00:00+07'
),
(
  (select id from public.blog_categories where slug = 'compare'),
  'So sánh eSIM du lịch và SIM vật lý: Lựa chọn nào tối ưu cho phượt thủ?',
  'so-sanh-esim-va-sim-vat-ly',
  'Phân tích chi tiết về giá cả, độ tiện dụng, khả năng chuyển vùng và tương thích thiết bị của hai hình thức kết nối.',
  '<p class="text-base md:text-lg leading-relaxed text-slate-600 font-medium">Khi chuẩn bị đi du lịch nước ngoài, lựa chọn giữa eSIM (SIM điện tử kỹ thuật số) và thẻ SIM vật lý truyền thống là băn khoăn của rất nhiều du khách. Hãy cùng chúng tôi so sánh chi tiết các khía cạnh dưới đây.</p>
<h2 class="text-2xl font-bold text-slate-800 mt-8">Bảng so sánh chi tiết: eSIM vs SIM vật lý</h2>
<div class="overflow-x-auto my-6 border border-slate-100 rounded-2xl shadow-sm">
  <table class="w-full text-sm text-left text-slate-600 border-collapse">
    <thead class="bg-slate-50 text-slate-800 uppercase font-bold text-xs border-b border-slate-200">
      <tr>
        <th class="px-6 py-4">Tiêu chí</th>
        <th class="px-6 py-4">eSIM Du Lịch</th>
        <th class="px-6 py-4">SIM Vật Lý Du Lịch</th>
      </tr>
    </thead>
    <tbody class="divide-y divide-slate-100">
      <tr>
        <td class="px-6 py-4 font-semibold text-slate-800">Thao tác lắp đặt</td>
        <td class="px-6 py-4 text-emerald-600 font-semibold">Quét mã QR online (Không cần tháo máy)</td>
        <td class="px-6 py-4 text-slate-500">Phải dùng que chọc SIM và thay đổi thẻ vật lý</td>
      </tr>
      <tr>
        <td class="px-6 py-4 font-semibold text-slate-800">Khả năng mất SIM gốc</td>
        <td class="px-6 py-4 text-emerald-600 font-semibold">Không (Giữ nguyên SIM gốc trong máy)</td>
        <td class="px-6 py-4 text-amber-600 font-semibold">Có (Dễ thất lạc thẻ SIM Việt Nam)</td>
      </tr>
      <tr>
        <td class="px-6 py-4 font-semibold text-slate-800">Thời gian nhận hàng</td>
        <td class="px-6 py-4 text-emerald-600 font-semibold">Trong 5 phút qua Email (24/7)</td>
        <td class="px-6 py-4 text-slate-500">Phải chờ ship COD hoặc nhận tại sân bay</td>
      </tr>
      <tr>
        <td class="px-6 py-4 font-semibold text-slate-800">Độ tương thích máy</td>
        <td class="px-6 py-4 text-amber-600 font-semibold">Chỉ hỗ trợ điện thoại thế hệ mới</td>
        <td class="px-6 py-4 text-emerald-600 font-semibold">Hỗ trợ 100% tất cả các loại điện thoại</td>
      </tr>
    </tbody>
  </table>
</div>
<h2 class="text-2xl font-bold text-slate-800 mt-8">Đánh giá chung: Bạn nên chọn loại nào?</h2>
<p class="text-slate-600 leading-relaxed">Nếu điện thoại của bạn hỗ trợ eSIM (các dòng máy sản xuất từ năm 2020 trở lại đây), chúng tôi khuyên bạn nên lựa chọn <strong>eSIM</strong> vì sự tiện ích, tốc độ nhận gói cước ngay lập tức và tính an toàn tuyệt đối cho khay SIM gốc Việt Nam. Đối với các dòng máy cũ hơn, <strong>SIM vật lý</strong> vẫn là lựa chọn bắt buộc giúp kết nối internet ổn định suốt hành trình.</p>',
  '/images/blog_image_3.png',
  'Trần Minh Tuấn',
  'published',
  '2026-06-15 00:00:00+07'
),
(
  (select id from public.blog_categories where slug = 'travel'),
  'Kinh nghiệm mua và sử dụng eSIM tại Nhật Bản: Những điều cần lưu ý',
  'kinh-nghiem-esim-tai-nhat-ban',
  'Giúp bạn tránh những lỗi thường gặp khi thiết lập kết nối mạng Docomo/SoftBank tại xứ sở mặt trời mọc.',
  '<p class="text-left text-slate-600 leading-relaxed">Hướng dẫn chi tiết về cách kích hoạt eSIM tại Nhật Bản và kết nối sóng nhà mạng Docomo / SoftBank...</p>',
  '/images/blog_image_4.png',
  'Lê Hoàng Hải',
  'published',
  '2026-06-14 00:00:00+07'
),
(
  (select id from public.blog_categories where slug = 'travel'),
  'Top 5 ứng dụng hữu ích nhất khi du lịch nước ngoài cần kết nối Internet',
  'top-5-ung-dung-du-lich-nuoc-ngoai',
  'Từ bản đồ số, dịch thuật trực tiếp đến các app gọi xe công nghệ tại khu vực Đông Nam Á và Châu Âu.',
  '<p class="text-left text-slate-600 leading-relaxed">Danh sách 5 ứng dụng đắc lực nhất giúp chuyến hành trình tự túc nước ngoài của bạn trở nên trơn tru nhất...</p>',
  '/images/blog_image_5.png',
  'Nguyễn Thị Vy',
  'published',
  '2026-06-12 00:00:00+07'
),
(
  (select id from public.blog_categories where slug = 'guides'),
  'Danh sách điện thoại hỗ trợ eSIM mới nhất năm 2026',
  'danh-sach-dien-thoai-ho-tro-esim-2026',
  'Cách tra cứu xem thiết bị iPhone, Samsung, Xiaomi của bạn có tích hợp vi mạch eSIM để mua sắm an tâm hơn.',
  '<p class="text-left text-slate-600 leading-relaxed">Bảng tổng hợp chi tiết tất cả các mẫu điện thoại tích hợp sẵn chip eSIM mới nhất hiện nay...</p>',
  '/images/blog_image_6.png',
  'Kỹ thuật viên SIMDULICH.VN',
  'published',
  '2026-06-10 00:00:00+07'
);
```

- [ ] **Step 2: Commit**

```bash
git add supabase/migrations/0010_blog.sql
git commit -m "feat: add blog_categories/blog_posts schema, RLS, and seed data"
```

---

### Task 2: Backend — blog categories (lib + routes)

**Files:**
- Create: `lib/blogCategories.js`
- Create: `lib/__tests__/blogCategories.test.js`
- Create: `app/api/blog/categories/route.js` (public GET)
- Create: `app/api/blog/categories/__tests__/route.test.js`
- Create: `app/api/blog/admin/categories/route.js` (admin GET list, POST create)
- Create: `app/api/blog/admin/categories/__tests__/route.test.js`
- Create: `app/api/blog/admin/categories/[id]/route.js` (admin PUT update, DELETE)
- Create: `app/api/blog/admin/categories/[id]/__tests__/route.test.js`

**Interfaces:**
- Consumes: `createApiClient` from `lib/supabase/apiClient.js`; `authenticate`, `authErrorResponse`,
  `requireRole` from `lib/apiAuth.js` (all existing, unmodified).
- Produces: `listCategoriesPublic()` (async, no args, returns `[{id, name, slug}, ...]`, never
  throws — returns `[]` on error, matching `lib/catalog.js`'s `listCategories()` precedent),
  `listCategoriesAdmin(supabase)`, `createCategoryAdmin(supabase, {name, slug})`,
  `updateCategoryAdmin(supabase, id, {name, slug})`, `deleteCategoryAdmin(supabase, id)` — all
  return `{data, error}` (or `{deleted, error}` for delete), mirroring
  `lib/adminCatalog.js`'s category functions exactly. Task 3 imports `mapCategory`-shaped fields
  are NOT reused across files — Task 3's `blog_posts` mapping embeds `categoryName` itself via a
  join, it does not call into this file.

- [ ] **Step 1: Write the failing test for `lib/blogCategories.js`**

Create `lib/__tests__/blogCategories.test.js`:

```js
import { describe, it, expect, vi, beforeEach } from 'vitest';

function createQueryBuilderMock(result) {
  const builder = {
    select: vi.fn(() => builder),
    insert: vi.fn(() => builder),
    update: vi.fn(() => builder),
    delete: vi.fn(() => builder),
    eq: vi.fn(() => builder),
    order: vi.fn(() => builder),
    maybeSingle: vi.fn(() => builder),
    then: (resolve) => resolve(result),
  };
  return builder;
}

const fromMock = vi.fn();
const supabaseMock = { from: fromMock };
const createApiClientMock = vi.fn();

vi.mock('../supabase/apiClient', () => ({
  createApiClient: (...args) => createApiClientMock(...args),
}));

import {
  listCategoriesPublic, listCategoriesAdmin, createCategoryAdmin, updateCategoryAdmin, deleteCategoryAdmin,
} from '../blogCategories';

describe('lib/blogCategories', () => {
  beforeEach(() => {
    fromMock.mockReset();
    createApiClientMock.mockReset();
  });

  describe('listCategoriesPublic', () => {
    it('trả mảng danh mục khi thành công', async () => {
      createApiClientMock.mockReturnValue(supabaseMock);
      fromMock.mockReturnValue(createQueryBuilderMock({
        data: [{ id: 1, name: 'Hướng dẫn cài đặt', slug: 'guides' }],
        error: null,
      }));

      const result = await listCategoriesPublic();

      expect(result).toEqual([{ id: 1, name: 'Hướng dẫn cài đặt', slug: 'guides' }]);
    });

    it('trả mảng rỗng khi Supabase chưa cấu hình', async () => {
      createApiClientMock.mockReturnValue(null);

      const result = await listCategoriesPublic();

      expect(result).toEqual([]);
    });

    it('trả mảng rỗng khi query lỗi (không throw)', async () => {
      createApiClientMock.mockReturnValue(supabaseMock);
      fromMock.mockReturnValue(createQueryBuilderMock({ data: null, error: { message: 'boom' } }));

      const result = await listCategoriesPublic();

      expect(result).toEqual([]);
    });
  });

  describe('listCategoriesAdmin', () => {
    it('trả {data, error}', async () => {
      fromMock.mockReturnValue(createQueryBuilderMock({
        data: [{ id: 1, name: 'Hướng dẫn cài đặt', slug: 'guides' }],
        error: null,
      }));

      const result = await listCategoriesAdmin(supabaseMock);

      expect(result).toEqual({ data: [{ id: 1, name: 'Hướng dẫn cài đặt', slug: 'guides' }], error: null });
    });
  });

  describe('createCategoryAdmin', () => {
    it('tạo danh mục thành công', async () => {
      fromMock.mockReturnValue(createQueryBuilderMock({
        data: { id: 4, name: 'Mới', slug: 'moi' },
        error: null,
      }));

      const result = await createCategoryAdmin(supabaseMock, { name: 'Mới', slug: 'moi' });

      expect(result).toEqual({ data: { id: 4, name: 'Mới', slug: 'moi' }, error: null });
    });

    it('trả error khi slug trùng', async () => {
      fromMock.mockReturnValue(createQueryBuilderMock({ data: null, error: { code: '23505' } }));

      const result = await createCategoryAdmin(supabaseMock, { name: 'Mới', slug: 'guides' });

      expect(result.data).toBeNull();
      expect(result.error.code).toBe('23505');
    });
  });

  describe('updateCategoryAdmin', () => {
    it('sửa danh mục thành công', async () => {
      fromMock.mockReturnValue(createQueryBuilderMock({
        data: { id: 1, name: 'Đổi tên', slug: 'guides' },
        error: null,
      }));

      const result = await updateCategoryAdmin(supabaseMock, 1, { name: 'Đổi tên', slug: 'guides' });

      expect(result).toEqual({ data: { id: 1, name: 'Đổi tên', slug: 'guides' }, error: null });
    });

    it('trả {data: null, error: null} khi không tìm thấy', async () => {
      fromMock.mockReturnValue(createQueryBuilderMock({ data: null, error: null }));

      const result = await updateCategoryAdmin(supabaseMock, 999, { name: 'X', slug: 'x' });

      expect(result).toEqual({ data: null, error: null });
    });
  });

  describe('deleteCategoryAdmin', () => {
    it('trả {deleted: true} khi xoá thành công', async () => {
      fromMock.mockReturnValue(createQueryBuilderMock({ data: [{ id: 1 }], error: null }));

      const result = await deleteCategoryAdmin(supabaseMock, 1);

      expect(result).toEqual({ deleted: true, error: null });
    });

    it('trả {deleted: false} khi không tìm thấy', async () => {
      fromMock.mockReturnValue(createQueryBuilderMock({ data: [], error: null }));

      const result = await deleteCategoryAdmin(supabaseMock, 999);

      expect(result).toEqual({ deleted: false, error: null });
    });

    it('trả error khi còn bài viết tham chiếu (FK)', async () => {
      fromMock.mockReturnValue(createQueryBuilderMock({ data: null, error: { code: '23503' } }));

      const result = await deleteCategoryAdmin(supabaseMock, 1);

      expect(result.deleted).toBe(false);
      expect(result.error.code).toBe('23503');
    });
  });
});
```

- [ ] **Step 2: Run test, confirm it fails**

Run: `npm test -- lib/__tests__/blogCategories.test.js`
Expected: FAIL with "Cannot find module '../blogCategories'".

- [ ] **Step 3: Create `lib/blogCategories.js`**

```js
import { createApiClient } from './supabase/apiClient';

function mapCategory(row) {
  return { id: row.id, name: row.name, slug: row.slug };
}

export async function listCategoriesPublic() {
  const supabase = createApiClient();
  if (!supabase) return [];
  const { data, error } = await supabase
    .from('blog_categories')
    .select('id, name, slug')
    .order('id', { ascending: true });
  if (error) {
    console.error('[blogCategories] listCategoriesPublic: failed to query categories', error);
    return [];
  }
  return (data || []).map(mapCategory);
}

export async function listCategoriesAdmin(supabase) {
  const { data, error } = await supabase
    .from('blog_categories')
    .select('id, name, slug')
    .order('id', { ascending: true });
  if (error) return { data: null, error };
  return { data: (data || []).map(mapCategory), error: null };
}

export async function createCategoryAdmin(supabase, { name, slug }) {
  const { data, error } = await supabase
    .from('blog_categories')
    .insert({ name, slug })
    .select('id, name, slug')
    .maybeSingle();
  if (error) return { data: null, error };
  return { data: mapCategory(data), error: null };
}

export async function updateCategoryAdmin(supabase, id, { name, slug }) {
  const { data, error } = await supabase
    .from('blog_categories')
    .update({ name, slug })
    .eq('id', id)
    .select('id, name, slug')
    .maybeSingle();
  if (error) return { data: null, error };
  if (!data) return { data: null, error: null };
  return { data: mapCategory(data), error: null };
}

export async function deleteCategoryAdmin(supabase, id) {
  const { data, error } = await supabase
    .from('blog_categories')
    .delete()
    .eq('id', id)
    .select('id');
  if (error) return { deleted: false, error };
  return { deleted: (data || []).length > 0, error: null };
}
```

- [ ] **Step 4: Run test, confirm it passes**

Run: `npm test -- lib/__tests__/blogCategories.test.js`
Expected: PASS (11 tests).

- [ ] **Step 5: Write the failing test for `GET /api/blog/categories`**

Create `app/api/blog/categories/__tests__/route.test.js`:

```js
import { describe, it, expect, vi, beforeEach } from 'vitest';

const listCategoriesPublicMock = vi.fn();

vi.mock('../../../../../lib/blogCategories', () => ({
  listCategoriesPublic: (...args) => listCategoriesPublicMock(...args),
}));

import { GET } from '../route';

describe('GET /api/blog/categories', () => {
  beforeEach(() => {
    listCategoriesPublicMock.mockReset();
  });

  it('trả mảng danh mục', async () => {
    listCategoriesPublicMock.mockResolvedValue([{ id: 1, name: 'Hướng dẫn cài đặt', slug: 'guides' }]);

    const response = await GET();
    const body = await response.json();

    expect(body).toEqual([{ id: 1, name: 'Hướng dẫn cài đặt', slug: 'guides' }]);
  });
});
```

- [ ] **Step 6: Run test, confirm it fails**

Run: `npm test -- app/api/blog/categories/__tests__/route.test.js`
Expected: FAIL with "Cannot find module '../route'".

- [ ] **Step 7: Create `app/api/blog/categories/route.js`**

```js
import { NextResponse } from 'next/server';
import { listCategoriesPublic } from '../../../../lib/blogCategories';

export async function GET() {
  const categories = await listCategoriesPublic();
  return NextResponse.json(categories);
}
```

- [ ] **Step 8: Run test, confirm it passes**

Run: `npm test -- app/api/blog/categories/__tests__/route.test.js`
Expected: PASS (1 test).

- [ ] **Step 9: Write the failing test for admin categories list/create**

Create `app/api/blog/admin/categories/__tests__/route.test.js`:

```js
import { describe, it, expect, vi, beforeEach } from 'vitest';

const authenticateMock = vi.fn();
const listCategoriesAdminMock = vi.fn();
const createCategoryAdminMock = vi.fn();

vi.mock('../../../../../../lib/apiAuth', async () => {
  const actual = await vi.importActual('../../../../../../lib/apiAuth');
  return { ...actual, authenticate: (...args) => authenticateMock(...args) };
});
vi.mock('../../../../../../lib/blogCategories', () => ({
  listCategoriesAdmin: (...args) => listCategoriesAdminMock(...args),
  createCategoryAdmin: (...args) => createCategoryAdminMock(...args),
}));

import { GET, POST } from '../route';

function makeGetRequest() {
  return { headers: { get: () => 'Bearer good-token' } };
}
function makePostRequest(body) {
  return { headers: { get: () => 'Bearer good-token' }, json: () => Promise.resolve(body) };
}

describe('GET /api/blog/admin/categories', () => {
  beforeEach(() => {
    authenticateMock.mockReset();
    listCategoriesAdminMock.mockReset();
    createCategoryAdminMock.mockReset();
  });

  it('trả danh sách khi caller là admin', async () => {
    authenticateMock.mockResolvedValue({ user: { role: 'admin' }, supabase: {} });
    listCategoriesAdminMock.mockResolvedValue({ data: [{ id: 1, name: 'A', slug: 'a' }], error: null });

    const response = await GET(makeGetRequest());
    const body = await response.json();

    expect(body).toEqual([{ id: 1, name: 'A', slug: 'a' }]);
  });

  it('trả 403 khi caller không phải admin/staff', async () => {
    authenticateMock.mockResolvedValue({ user: { role: 'customer' }, supabase: {} });

    const response = await GET(makeGetRequest());

    expect(response.status).toBe(403);
  });
});

describe('POST /api/blog/admin/categories', () => {
  beforeEach(() => {
    authenticateMock.mockReset();
    createCategoryAdminMock.mockReset();
  });

  it('tạo danh mục thành công, trả 201', async () => {
    authenticateMock.mockResolvedValue({ user: { role: 'admin' }, supabase: {} });
    createCategoryAdminMock.mockResolvedValue({ data: { id: 4, name: 'Mới', slug: 'moi' }, error: null });

    const response = await POST(makePostRequest({ name: 'Mới', slug: 'moi' }));
    const body = await response.json();

    expect(response.status).toBe(201);
    expect(body).toEqual({ id: 4, name: 'Mới', slug: 'moi' });
  });

  it('trả 400 khi thiếu name/slug', async () => {
    authenticateMock.mockResolvedValue({ user: { role: 'admin' }, supabase: {} });

    const response = await POST(makePostRequest({ name: '' }));

    expect(response.status).toBe(400);
    expect(createCategoryAdminMock).not.toHaveBeenCalled();
  });

  it('trả 400 khi slug trùng', async () => {
    authenticateMock.mockResolvedValue({ user: { role: 'admin' }, supabase: {} });
    createCategoryAdminMock.mockResolvedValue({ data: null, error: { code: '23505' } });

    const response = await POST(makePostRequest({ name: 'Mới', slug: 'guides' }));
    const body = await response.json();

    expect(response.status).toBe(400);
    expect(body).toEqual({ message: 'Slug đã tồn tại, vui lòng chọn slug khác.' });
  });
});
```

- [ ] **Step 10: Run test, confirm it fails**

Run: `npm test -- app/api/blog/admin/categories/__tests__/route.test.js`
Expected: FAIL with "Cannot find module '../route'".

- [ ] **Step 11: Create `app/api/blog/admin/categories/route.js`**

```js
import { NextResponse } from 'next/server';
import { authenticate, authErrorResponse, requireRole } from '../../../../../lib/apiAuth';
import { listCategoriesAdmin, createCategoryAdmin } from '../../../../../lib/blogCategories';

export async function GET(request) {
  try {
    const { user, supabase } = await authenticate(request);
    requireRole(user, ['admin', 'staff']);

    const { data, error } = await listCategoriesAdmin(supabase);
    if (error) {
      return NextResponse.json({ message: 'Không tải được danh sách danh mục.' }, { status: 500 });
    }
    return NextResponse.json(data);
  } catch (error) {
    return authErrorResponse(error);
  }
}

export async function POST(request) {
  try {
    const { user, supabase } = await authenticate(request);
    requireRole(user, ['admin', 'staff']);

    const { name, slug } = await request.json();
    if (!name || !slug) {
      return NextResponse.json({ message: 'Thiếu thông tin bắt buộc (name/slug).' }, { status: 400 });
    }

    const { data, error } = await createCategoryAdmin(supabase, { name, slug });
    if (error) {
      if (error.code === '23505') {
        return NextResponse.json({ message: 'Slug đã tồn tại, vui lòng chọn slug khác.' }, { status: 400 });
      }
      return NextResponse.json({ message: 'Không tạo được danh mục.' }, { status: 500 });
    }
    return NextResponse.json(data, { status: 201 });
  } catch (error) {
    return authErrorResponse(error);
  }
}
```

- [ ] **Step 12: Run test, confirm it passes**

Run: `npm test -- app/api/blog/admin/categories/__tests__/route.test.js`
Expected: PASS (4 tests).

- [ ] **Step 13: Write the failing test for admin category update/delete**

Create `app/api/blog/admin/categories/[id]/__tests__/route.test.js`:

```js
import { describe, it, expect, vi, beforeEach } from 'vitest';

const authenticateMock = vi.fn();
const updateCategoryAdminMock = vi.fn();
const deleteCategoryAdminMock = vi.fn();

vi.mock('../../../../../../../lib/apiAuth', async () => {
  const actual = await vi.importActual('../../../../../../../lib/apiAuth');
  return { ...actual, authenticate: (...args) => authenticateMock(...args) };
});
vi.mock('../../../../../../../lib/blogCategories', () => ({
  updateCategoryAdmin: (...args) => updateCategoryAdminMock(...args),
  deleteCategoryAdmin: (...args) => deleteCategoryAdminMock(...args),
}));

import { PUT, DELETE } from '../route';

function makeRequest(body) {
  return { headers: { get: () => 'Bearer good-token' }, json: () => Promise.resolve(body) };
}

describe('PUT /api/blog/admin/categories/[id]', () => {
  beforeEach(() => {
    authenticateMock.mockReset();
    updateCategoryAdminMock.mockReset();
  });

  it('sửa thành công', async () => {
    authenticateMock.mockResolvedValue({ user: { role: 'admin' }, supabase: {} });
    updateCategoryAdminMock.mockResolvedValue({ data: { id: 1, name: 'Đổi tên', slug: 'guides' }, error: null });

    const response = await PUT(makeRequest({ name: 'Đổi tên', slug: 'guides' }), { params: Promise.resolve({ id: '1' }) });
    const body = await response.json();

    expect(body).toEqual({ id: 1, name: 'Đổi tên', slug: 'guides' });
  });

  it('trả 404 khi không tìm thấy', async () => {
    authenticateMock.mockResolvedValue({ user: { role: 'admin' }, supabase: {} });
    updateCategoryAdminMock.mockResolvedValue({ data: null, error: null });

    const response = await PUT(makeRequest({ name: 'X', slug: 'x' }), { params: Promise.resolve({ id: '999' }) });

    expect(response.status).toBe(404);
  });
});

describe('DELETE /api/blog/admin/categories/[id]', () => {
  beforeEach(() => {
    authenticateMock.mockReset();
    deleteCategoryAdminMock.mockReset();
  });

  it('xoá thành công, trả 204', async () => {
    authenticateMock.mockResolvedValue({ user: { role: 'admin' }, supabase: {} });
    deleteCategoryAdminMock.mockResolvedValue({ deleted: true, error: null });

    const response = await DELETE(makeRequest(), { params: Promise.resolve({ id: '1' }) });

    expect(response.status).toBe(204);
  });

  it('trả 400 khi còn bài viết tham chiếu', async () => {
    authenticateMock.mockResolvedValue({ user: { role: 'admin' }, supabase: {} });
    deleteCategoryAdminMock.mockResolvedValue({ deleted: false, error: { code: '23503' } });

    const response = await DELETE(makeRequest(), { params: Promise.resolve({ id: '1' }) });
    const body = await response.json();

    expect(response.status).toBe(400);
    expect(body).toEqual({ message: 'Không thể xoá — danh mục còn bài viết liên kết.' });
  });
});
```

- [ ] **Step 14: Run test, confirm it fails**

Run: `npm test -- "app/api/blog/admin/categories/\[id\]"`
Expected: FAIL with "Cannot find module '../route'".

- [ ] **Step 15: Create `app/api/blog/admin/categories/[id]/route.js`**

```js
import { NextResponse } from 'next/server';
import { authenticate, authErrorResponse, requireRole } from '../../../../../../lib/apiAuth';
import { updateCategoryAdmin, deleteCategoryAdmin } from '../../../../../../lib/blogCategories';

export async function PUT(request, { params }) {
  try {
    const { user, supabase } = await authenticate(request);
    requireRole(user, ['admin', 'staff']);
    const { id } = await params;

    const { name, slug } = await request.json();
    if (!name || !slug) {
      return NextResponse.json({ message: 'Thiếu thông tin bắt buộc (name/slug).' }, { status: 400 });
    }

    const { data, error } = await updateCategoryAdmin(supabase, id, { name, slug });
    if (error) {
      if (error.code === '23505') {
        return NextResponse.json({ message: 'Slug đã tồn tại, vui lòng chọn slug khác.' }, { status: 400 });
      }
      return NextResponse.json({ message: 'Không cập nhật được danh mục.' }, { status: 500 });
    }
    if (!data) {
      return NextResponse.json({ message: 'Không tìm thấy danh mục.' }, { status: 404 });
    }
    return NextResponse.json(data);
  } catch (error) {
    return authErrorResponse(error);
  }
}

export async function DELETE(request, { params }) {
  try {
    const { user, supabase } = await authenticate(request);
    requireRole(user, ['admin', 'staff']);
    const { id } = await params;

    const { deleted, error } = await deleteCategoryAdmin(supabase, id);
    if (error) {
      if (error.code === '23503') {
        return NextResponse.json({ message: 'Không thể xoá — danh mục còn bài viết liên kết.' }, { status: 400 });
      }
      return NextResponse.json({ message: 'Không xoá được danh mục.' }, { status: 500 });
    }
    if (!deleted) {
      return NextResponse.json({ message: 'Không tìm thấy danh mục.' }, { status: 404 });
    }
    return new NextResponse(null, { status: 204 });
  } catch (error) {
    return authErrorResponse(error);
  }
}
```

- [ ] **Step 16: Run test, confirm it passes**

Run: `npm test -- "app/api/blog/admin/categories/\[id\]"`
Expected: PASS (4 tests).

- [ ] **Step 17: Run the full suite, then commit**

Run: `npm test`
Expected: PASS.

```bash
git add lib/blogCategories.js lib/__tests__/blogCategories.test.js app/api/blog/categories app/api/blog/admin/categories
git commit -m "feat: add blog categories public read + admin CRUD"
```

---

### Task 3: Backend — blog posts (lib + routes)

**Files:**
- Create: `lib/blogPosts.js`
- Create: `lib/__tests__/blogPosts.test.js`
- Create: `app/api/blog/posts/route.js` (public list)
- Create: `app/api/blog/posts/__tests__/route.test.js`
- Create: `app/api/blog/posts/[slug]/route.js` (public detail)
- Create: `app/api/blog/posts/[slug]/__tests__/route.test.js`
- Create: `app/api/blog/admin/posts/route.js` (admin list, create)
- Create: `app/api/blog/admin/posts/__tests__/route.test.js`
- Create: `app/api/blog/admin/posts/[id]/route.js` (admin detail, update, delete)
- Create: `app/api/blog/admin/posts/[id]/__tests__/route.test.js`

**Interfaces:**
- Consumes: `createApiClient` from `lib/supabase/apiClient.js`; `authenticate`, `authErrorResponse`,
  `requireRole` from `lib/apiAuth.js` (existing, unmodified). Does NOT import from
  `lib/blogCategories.js` (Task 2) — posts join `blog_categories` directly in their own queries for
  `categoryName`, keeping the two lib files independent (matches the plan's module-boundary intent,
  same as `lib/orders.js`/`lib/adminOrders.js` in the Order/Payment API not sharing row-mapping
  code across files).
- Produces: `listPostsPublic()` (async, no args, `[{id, categoryId, categoryName, title, slug,
  excerpt, imageUrl, author, publishedAt}, ...]`, never throws), `getPostBySlugPublic(slug)` (async,
  full post shape including `content`, or `null` if not found/not published, never throws),
  `listPostsAdmin(supabase, {page, size})`, `getPostAdminById(supabase, id)`,
  `createPostAdmin(supabase, {categoryId, title, slug, excerpt, content, imageUrl, author,
  status})`, `updatePostAdmin(supabase, id, {...same fields...})`, `deletePostAdmin(supabase, id)`.

- [ ] **Step 1: Write the failing test for `lib/blogPosts.js`**

Create `lib/__tests__/blogPosts.test.js`:

```js
import { describe, it, expect, vi, beforeEach } from 'vitest';

function createQueryBuilderMock(result) {
  const builder = {
    select: vi.fn(() => builder),
    insert: vi.fn(() => builder),
    update: vi.fn(() => builder),
    delete: vi.fn(() => builder),
    eq: vi.fn(() => builder),
    order: vi.fn(() => builder),
    range: vi.fn(() => builder),
    maybeSingle: vi.fn(() => builder),
    then: (resolve) => resolve(result),
  };
  return builder;
}

const fromMock = vi.fn();
const supabaseMock = { from: fromMock };
const createApiClientMock = vi.fn();

vi.mock('../supabase/apiClient', () => ({
  createApiClient: (...args) => createApiClientMock(...args),
}));

import {
  listPostsPublic, getPostBySlugPublic, listPostsAdmin, getPostAdminById,
  createPostAdmin, updatePostAdmin, deletePostAdmin,
} from '../blogPosts';

const ROW = {
  id: 1, category_id: 2, blog_categories: { name: 'Kinh nghiệm du lịch' },
  title: 'Bài test', slug: 'bai-test', excerpt: 'Mô tả ngắn', content: '<p>Nội dung</p>',
  image_url: '/images/x.png', author: 'Tác giả', status: 'published',
  published_at: '2026-06-15T00:00:00+07:00', created_at: '2026-06-01T00:00:00+07:00', updated_at: '2026-06-01T00:00:00+07:00',
};

describe('lib/blogPosts', () => {
  beforeEach(() => {
    fromMock.mockReset();
    createApiClientMock.mockReset();
  });

  describe('listPostsPublic', () => {
    it('trả mảng bài viết đã map đúng field', async () => {
      createApiClientMock.mockReturnValue(supabaseMock);
      fromMock.mockReturnValue(createQueryBuilderMock({ data: [ROW], error: null }));

      const result = await listPostsPublic();

      expect(result).toEqual([{
        id: 1, categoryId: 2, categoryName: 'Kinh nghiệm du lịch', title: 'Bài test', slug: 'bai-test',
        excerpt: 'Mô tả ngắn', imageUrl: '/images/x.png', author: 'Tác giả', publishedAt: '2026-06-15T00:00:00+07:00',
      }]);
    });

    it('trả mảng rỗng khi lỗi (không throw)', async () => {
      createApiClientMock.mockReturnValue(supabaseMock);
      fromMock.mockReturnValue(createQueryBuilderMock({ data: null, error: { message: 'boom' } }));

      const result = await listPostsPublic();

      expect(result).toEqual([]);
    });
  });

  describe('getPostBySlugPublic', () => {
    it('trả chi tiết đầy đủ kèm content khi tìm thấy', async () => {
      createApiClientMock.mockReturnValue(supabaseMock);
      fromMock.mockReturnValue(createQueryBuilderMock({ data: ROW, error: null }));

      const result = await getPostBySlugPublic('bai-test');

      expect(result).toMatchObject({ slug: 'bai-test', content: '<p>Nội dung</p>', categoryName: 'Kinh nghiệm du lịch' });
    });

    it('trả null khi không tìm thấy', async () => {
      createApiClientMock.mockReturnValue(supabaseMock);
      fromMock.mockReturnValue(createQueryBuilderMock({ data: null, error: null }));

      const result = await getPostBySlugPublic('khong-ton-tai');

      expect(result).toBeNull();
    });
  });

  describe('listPostsAdmin', () => {
    it('phân trang 0-indexed, trả field number', async () => {
      const query = createQueryBuilderMock({ data: [ROW], count: 1, error: null });
      fromMock.mockReturnValue(query);

      const result = await listPostsAdmin(supabaseMock, { page: 0, size: 20 });

      expect(query.range).toHaveBeenCalledWith(0, 19);
      expect(result.data.number).toBe(0);
      expect(result.data.content[0].slug).toBe('bai-test');
    });
  });

  describe('getPostAdminById', () => {
    it('trả chi tiết khi tìm thấy', async () => {
      fromMock.mockReturnValue(createQueryBuilderMock({ data: ROW, error: null }));

      const result = await getPostAdminById(supabaseMock, 1);

      expect(result.data.title).toBe('Bài test');
    });

    it('trả {data: null, error: null} khi không tìm thấy', async () => {
      fromMock.mockReturnValue(createQueryBuilderMock({ data: null, error: null }));

      const result = await getPostAdminById(supabaseMock, 999);

      expect(result).toEqual({ data: null, error: null });
    });
  });

  describe('createPostAdmin', () => {
    it('tạo bài thành công', async () => {
      fromMock.mockReturnValue(createQueryBuilderMock({ data: ROW, error: null }));

      const result = await createPostAdmin(supabaseMock, {
        categoryId: 2, title: 'Bài test', slug: 'bai-test', excerpt: 'Mô tả ngắn',
        content: '<p>Nội dung</p>', imageUrl: '/images/x.png', author: 'Tác giả', status: 'published',
      });

      expect(result.data.slug).toBe('bai-test');
    });

    it('trả error khi slug trùng', async () => {
      fromMock.mockReturnValue(createQueryBuilderMock({ data: null, error: { code: '23505' } }));

      const result = await createPostAdmin(supabaseMock, {
        categoryId: 2, title: 'X', slug: 'bai-test', excerpt: 'x', content: 'x', author: 'x', status: 'draft',
      });

      expect(result.data).toBeNull();
      expect(result.error.code).toBe('23505');
    });
  });

  describe('updatePostAdmin', () => {
    it('sửa bài thành công', async () => {
      fromMock.mockReturnValue(createQueryBuilderMock({ data: { ...ROW, title: 'Đổi tên' }, error: null }));

      const result = await updatePostAdmin(supabaseMock, 1, {
        categoryId: 2, title: 'Đổi tên', slug: 'bai-test', excerpt: 'x', content: 'x', author: 'x', status: 'published',
      });

      expect(result.data.title).toBe('Đổi tên');
    });
  });

  describe('deletePostAdmin', () => {
    it('trả {deleted: true} khi xoá thành công', async () => {
      fromMock.mockReturnValue(createQueryBuilderMock({ data: [{ id: 1 }], error: null }));

      const result = await deletePostAdmin(supabaseMock, 1);

      expect(result).toEqual({ deleted: true, error: null });
    });
  });
});
```

- [ ] **Step 2: Run test, confirm it fails**

Run: `npm test -- lib/__tests__/blogPosts.test.js`
Expected: FAIL with "Cannot find module '../blogPosts'".

- [ ] **Step 3: Create `lib/blogPosts.js`**

```js
import { createApiClient } from './supabase/apiClient';

const PUBLIC_LIST_COLUMNS = 'id, category_id, title, slug, excerpt, image_url, author, published_at, blog_categories(name)';
const FULL_COLUMNS = 'id, category_id, title, slug, excerpt, content, image_url, author, status, published_at, created_at, updated_at, blog_categories(name)';

function mapPostSummary(row) {
  return {
    id: row.id,
    categoryId: row.category_id,
    categoryName: row.blog_categories?.name ?? null,
    title: row.title,
    slug: row.slug,
    excerpt: row.excerpt,
    imageUrl: row.image_url,
    author: row.author,
    publishedAt: row.published_at,
  };
}

function mapPostDetail(row) {
  return {
    ...mapPostSummary(row),
    content: row.content,
    status: row.status,
    createdAt: row.created_at,
    updatedAt: row.updated_at,
  };
}

export async function listPostsPublic() {
  const supabase = createApiClient();
  if (!supabase) return [];
  const { data, error } = await supabase
    .from('blog_posts')
    .select(PUBLIC_LIST_COLUMNS)
    .eq('status', 'published')
    .order('published_at', { ascending: false });
  if (error) {
    console.error('[blogPosts] listPostsPublic: failed to query posts', error);
    return [];
  }
  return (data || []).map(mapPostSummary);
}

export async function getPostBySlugPublic(slug) {
  const supabase = createApiClient();
  if (!supabase) return null;
  const { data, error } = await supabase
    .from('blog_posts')
    .select(FULL_COLUMNS)
    .eq('slug', slug)
    .eq('status', 'published')
    .maybeSingle();
  if (error) {
    console.error('[blogPosts] getPostBySlugPublic: failed to query post', error);
    return null;
  }
  if (!data) return null;
  return mapPostDetail(data);
}

export async function listPostsAdmin(supabase, { page, size } = {}) {
  const safePage = Number.isFinite(page) && page >= 0 ? Math.trunc(page) : 0;
  const safeSize = Number.isFinite(size) && size > 0 ? Math.trunc(size) : 20;
  const from = safePage * safeSize;
  const to = from + safeSize - 1;

  const { data, count, error } = await supabase
    .from('blog_posts')
    .select(FULL_COLUMNS, { count: 'exact' })
    .order('created_at', { ascending: false })
    .range(from, to);
  if (error) return { data: null, error };

  const totalElements = count || 0;
  return {
    data: {
      content: (data || []).map(mapPostDetail),
      number: safePage,
      size: safeSize,
      totalElements,
      totalPages: Math.ceil(totalElements / safeSize),
    },
    error: null,
  };
}

export async function getPostAdminById(supabase, id) {
  const { data, error } = await supabase
    .from('blog_posts')
    .select(FULL_COLUMNS)
    .eq('id', id)
    .maybeSingle();
  if (error) return { data: null, error };
  if (!data) return { data: null, error: null };
  return { data: mapPostDetail(data), error: null };
}

function postFieldsToRow({ categoryId, title, slug, excerpt, content, imageUrl, author, status }) {
  const row = {
    category_id: categoryId,
    title,
    slug,
    excerpt,
    content,
    image_url: imageUrl ?? null,
    author,
    status,
  };
  if (status === 'published') {
    row.published_at = new Date().toISOString();
  }
  return row;
}

export async function createPostAdmin(supabase, fields) {
  const { data, error } = await supabase
    .from('blog_posts')
    .insert(postFieldsToRow(fields))
    .select(FULL_COLUMNS)
    .maybeSingle();
  if (error) return { data: null, error };
  return { data: mapPostDetail(data), error: null };
}

export async function updatePostAdmin(supabase, id, fields) {
  const row = postFieldsToRow(fields);
  row.updated_at = new Date().toISOString();
  // Không ghi đè published_at nếu bài đã có sẵn giá trị này từ trước (sửa 1 bài đã đăng không
  // được đẩy ngày đăng lên hiện tại) — chỉ set khi field status trong request là 'published' VÀ
  // bài hiện tại đang là draft (chuyển draft -> published lần đầu). Cách đơn giản và đúng nhất:
  // không set published_at ở đây nữa, để route gọi kèm cờ riêng — xem Task 3 Step 3 ghi chú dưới
  // route update.
  delete row.published_at;
  const { data, error } = await supabase
    .from('blog_posts')
    .update(row)
    .eq('id', id)
    .select(FULL_COLUMNS)
    .maybeSingle();
  if (error) return { data: null, error };
  if (!data) return { data: null, error: null };
  return { data: mapPostDetail(data), error: null };
}

export async function deletePostAdmin(supabase, id) {
  const { data, error } = await supabase
    .from('blog_posts')
    .delete()
    .eq('id', id)
    .select('id');
  if (error) return { deleted: false, error };
  return { deleted: (data || []).length > 0, error: null };
}
```

**Note on `published_at` for updates:** the brief above deliberately makes `updatePostAdmin` never
touch `published_at` — this keeps the lib function simple and matches the spec's rule ("giữ nguyên
nếu đã có, không bị ghi đè mỗi lần sửa bài đã đăng"). The one gap this leaves — a `draft` post being
published for the FIRST time via `PUT` never gets a `published_at` set at all, so it would never
show up in the public list (which is ordered by `published_at desc` and implicitly relies on it
being non-null once `status = 'published'`) — is handled by the admin UI (Task 6): the editor
always uses `createPostAdmin` for brand-new posts (which does set `published_at` when created
directly as `published`), and when flipping an existing draft to published via the editor's "Đăng
bài" button, the editor's own submit handler will need to detect this transition. Flag this
explicitly to the Task 3 implementer: **add one more explicit rule to `updatePostAdmin`** instead
of leaving it to the frontend — before running Step 3's code as final, first fetch the current row
to check whether it's transitioning from `draft` to `published`, and set `published_at` only in
that case. Rewrite `updatePostAdmin` as:

```js
export async function updatePostAdmin(supabase, id, fields) {
  const { data: existing, error: fetchError } = await supabase
    .from('blog_posts')
    .select('status, published_at')
    .eq('id', id)
    .maybeSingle();
  if (fetchError) return { data: null, error: fetchError };
  if (!existing) return { data: null, error: null };

  const row = postFieldsToRow(fields);
  const isFirstPublish = fields.status === 'published' && existing.status !== 'published' && !existing.published_at;
  if (!isFirstPublish) {
    delete row.published_at;
  }

  const { data, error } = await supabase
    .from('blog_posts')
    .update(row)
    .eq('id', id)
    .select(FULL_COLUMNS)
    .maybeSingle();
  if (error) return { data: null, error };
  if (!data) return { data: null, error: null };
  return { data: mapPostDetail(data), error: null };
}
```

Use THIS version (with the existing-row fetch) as the actual implementation, not the simpler one
shown earlier in this step — the simpler one was shown first only to explain the problem it has.
Add a test for this: updating a `draft` post with `status: 'published'` sets `published_at` to a
non-null value (mock the initial fetch to return `{status: 'draft', published_at: null}`, and
assert the `update` call's argument includes a `published_at` key); updating an already-`published`
post (with any field change, `status` staying `'published'`) does NOT include `published_at` in the
`update` call's argument (mock the initial fetch to return `{status: 'published', published_at:
'2026-06-01T00:00:00Z'}`).

- [ ] **Step 4: Run test, confirm it passes**

Run: `npm test -- lib/__tests__/blogPosts.test.js`
Expected: PASS (all tests, including the two new `updatePostAdmin` published_at tests you just
added per the note above).

- [ ] **Step 5: Write the failing test for `GET /api/blog/posts`**

Create `app/api/blog/posts/__tests__/route.test.js`:

```js
import { describe, it, expect, vi, beforeEach } from 'vitest';

const listPostsPublicMock = vi.fn();

vi.mock('../../../../../lib/blogPosts', () => ({
  listPostsPublic: (...args) => listPostsPublicMock(...args),
}));

import { GET } from '../route';

describe('GET /api/blog/posts', () => {
  beforeEach(() => {
    listPostsPublicMock.mockReset();
  });

  it('trả mảng bài viết', async () => {
    listPostsPublicMock.mockResolvedValue([{ id: 1, title: 'Bài test', slug: 'bai-test' }]);

    const response = await GET();
    const body = await response.json();

    expect(body).toEqual([{ id: 1, title: 'Bài test', slug: 'bai-test' }]);
  });
});
```

- [ ] **Step 6: Run test, confirm it fails**

Run: `npm test -- app/api/blog/posts/__tests__/route.test.js`
Expected: FAIL with "Cannot find module '../route'".

- [ ] **Step 7: Create `app/api/blog/posts/route.js`**

```js
import { NextResponse } from 'next/server';
import { listPostsPublic } from '../../../../lib/blogPosts';

export async function GET() {
  const posts = await listPostsPublic();
  return NextResponse.json(posts);
}
```

- [ ] **Step 8: Run test, confirm it passes**

Run: `npm test -- app/api/blog/posts/__tests__/route.test.js`
Expected: PASS (1 test).

- [ ] **Step 9: Write the failing test for `GET /api/blog/posts/[slug]`**

Create `app/api/blog/posts/[slug]/__tests__/route.test.js`:

```js
import { describe, it, expect, vi, beforeEach } from 'vitest';

const getPostBySlugPublicMock = vi.fn();

vi.mock('../../../../../../lib/blogPosts', () => ({
  getPostBySlugPublic: (...args) => getPostBySlugPublicMock(...args),
}));

import { GET } from '../route';

describe('GET /api/blog/posts/[slug]', () => {
  beforeEach(() => {
    getPostBySlugPublicMock.mockReset();
  });

  it('trả chi tiết khi tìm thấy', async () => {
    getPostBySlugPublicMock.mockResolvedValue({ id: 1, slug: 'bai-test', content: '<p>x</p>' });

    const response = await GET({}, { params: Promise.resolve({ slug: 'bai-test' }) });
    const body = await response.json();

    expect(body.slug).toBe('bai-test');
  });

  it('trả 404 khi không tìm thấy', async () => {
    getPostBySlugPublicMock.mockResolvedValue(null);

    const response = await GET({}, { params: Promise.resolve({ slug: 'khong-ton-tai' }) });
    const body = await response.json();

    expect(response.status).toBe(404);
    expect(body).toEqual({ message: 'Không tìm thấy bài viết.' });
  });
});
```

- [ ] **Step 10: Run test, confirm it fails**

Run: `npm test -- "app/api/blog/posts/\[slug\]"`
Expected: FAIL with "Cannot find module '../route'".

- [ ] **Step 11: Create `app/api/blog/posts/[slug]/route.js`**

```js
import { NextResponse } from 'next/server';
import { getPostBySlugPublic } from '../../../../../lib/blogPosts';

export async function GET(request, { params }) {
  const { slug } = await params;
  const post = await getPostBySlugPublic(slug);
  if (!post) {
    return NextResponse.json({ message: 'Không tìm thấy bài viết.' }, { status: 404 });
  }
  return NextResponse.json(post);
}
```

- [ ] **Step 12: Run test, confirm it passes**

Run: `npm test -- "app/api/blog/posts/\[slug\]"`
Expected: PASS (2 tests).

- [ ] **Step 13: Write the failing test for admin posts list/create**

Create `app/api/blog/admin/posts/__tests__/route.test.js`:

```js
import { describe, it, expect, vi, beforeEach } from 'vitest';

const authenticateMock = vi.fn();
const listPostsAdminMock = vi.fn();
const createPostAdminMock = vi.fn();

vi.mock('../../../../../../lib/apiAuth', async () => {
  const actual = await vi.importActual('../../../../../../lib/apiAuth');
  return { ...actual, authenticate: (...args) => authenticateMock(...args) };
});
vi.mock('../../../../../../lib/blogPosts', () => ({
  listPostsAdmin: (...args) => listPostsAdminMock(...args),
  createPostAdmin: (...args) => createPostAdminMock(...args),
}));

import { GET, POST } from '../route';

function makeGetRequest(url) {
  return { url, headers: { get: () => 'Bearer good-token' } };
}
function makePostRequest(body) {
  return { headers: { get: () => 'Bearer good-token' }, json: () => Promise.resolve(body) };
}

describe('GET /api/blog/admin/posts', () => {
  beforeEach(() => {
    authenticateMock.mockReset();
    listPostsAdminMock.mockReset();
  });

  it('đọc page/size từ query string', async () => {
    authenticateMock.mockResolvedValue({ user: { role: 'admin' }, supabase: {} });
    listPostsAdminMock.mockResolvedValue({ data: { content: [], number: 1, size: 10, totalElements: 0, totalPages: 0 }, error: null });

    await GET(makeGetRequest('http://localhost:3000/api/blog/admin/posts?page=1&size=10'));

    expect(listPostsAdminMock).toHaveBeenCalledWith({}, { page: 1, size: 10 });
  });

  it('trả 403 khi không phải admin/staff', async () => {
    authenticateMock.mockResolvedValue({ user: { role: 'customer' }, supabase: {} });

    const response = await GET(makeGetRequest('http://localhost:3000/api/blog/admin/posts'));

    expect(response.status).toBe(403);
  });
});

describe('POST /api/blog/admin/posts', () => {
  beforeEach(() => {
    authenticateMock.mockReset();
    createPostAdminMock.mockReset();
  });

  it('tạo bài thành công, trả 201', async () => {
    authenticateMock.mockResolvedValue({ user: { role: 'admin' }, supabase: {} });
    createPostAdminMock.mockResolvedValue({ data: { id: 7, slug: 'bai-moi' }, error: null });

    const response = await POST(makePostRequest({
      categoryId: 1, title: 'Bài mới', slug: 'bai-moi', excerpt: 'x', content: '<p>x</p>', author: 'A', status: 'draft',
    }));
    const body = await response.json();

    expect(response.status).toBe(201);
    expect(body.slug).toBe('bai-moi');
  });

  it('trả 400 khi thiếu field bắt buộc', async () => {
    authenticateMock.mockResolvedValue({ user: { role: 'admin' }, supabase: {} });

    const response = await POST(makePostRequest({ title: '' }));

    expect(response.status).toBe(400);
    expect(createPostAdminMock).not.toHaveBeenCalled();
  });

  it('trả 400 khi slug trùng', async () => {
    authenticateMock.mockResolvedValue({ user: { role: 'admin' }, supabase: {} });
    createPostAdminMock.mockResolvedValue({ data: null, error: { code: '23505' } });

    const response = await POST(makePostRequest({
      categoryId: 1, title: 'X', slug: 'trung', excerpt: 'x', content: 'x', author: 'A', status: 'draft',
    }));
    const body = await response.json();

    expect(response.status).toBe(400);
    expect(body).toEqual({ message: 'Slug đã tồn tại, vui lòng chọn slug khác.' });
  });

  it('trả 400 khi categoryId không hợp lệ (lỗi khoá ngoại)', async () => {
    authenticateMock.mockResolvedValue({ user: { role: 'admin' }, supabase: {} });
    createPostAdminMock.mockResolvedValue({ data: null, error: { code: '23503' } });

    const response = await POST(makePostRequest({
      categoryId: 999, title: 'X', slug: 'x', excerpt: 'x', content: 'x', author: 'A', status: 'draft',
    }));
    const body = await response.json();

    expect(response.status).toBe(400);
    expect(body).toEqual({ message: 'Danh mục không hợp lệ.' });
  });
});
```

- [ ] **Step 14: Run test, confirm it fails**

Run: `npm test -- app/api/blog/admin/posts/__tests__/route.test.js`
Expected: FAIL with "Cannot find module '../route'".

- [ ] **Step 15: Create `app/api/blog/admin/posts/route.js`**

```js
import { NextResponse } from 'next/server';
import { authenticate, authErrorResponse, requireRole } from '../../../../../lib/apiAuth';
import { listPostsAdmin, createPostAdmin } from '../../../../../lib/blogPosts';

const REQUIRED_FIELDS = ['categoryId', 'title', 'slug', 'excerpt', 'content', 'author', 'status'];

export async function GET(request) {
  try {
    const { user, supabase } = await authenticate(request);
    requireRole(user, ['admin', 'staff']);

    const { searchParams } = new URL(request.url);
    const { data, error } = await listPostsAdmin(supabase, {
      page: searchParams.get('page') != null ? Number(searchParams.get('page')) : undefined,
      size: searchParams.get('size') != null ? Number(searchParams.get('size')) : undefined,
    });
    if (error) {
      return NextResponse.json({ message: 'Không tải được danh sách bài viết.' }, { status: 500 });
    }
    return NextResponse.json(data);
  } catch (error) {
    return authErrorResponse(error);
  }
}

export async function POST(request) {
  try {
    const { user, supabase } = await authenticate(request);
    requireRole(user, ['admin', 'staff']);

    const body = await request.json();
    const missing = REQUIRED_FIELDS.some((field) => !body[field]);
    if (missing) {
      return NextResponse.json({ message: 'Thiếu thông tin bắt buộc.' }, { status: 400 });
    }

    const { data, error } = await createPostAdmin(supabase, body);
    if (error) {
      if (error.code === '23505') {
        return NextResponse.json({ message: 'Slug đã tồn tại, vui lòng chọn slug khác.' }, { status: 400 });
      }
      if (error.code === '23503') {
        return NextResponse.json({ message: 'Danh mục không hợp lệ.' }, { status: 400 });
      }
      return NextResponse.json({ message: 'Không tạo được bài viết.' }, { status: 500 });
    }
    return NextResponse.json(data, { status: 201 });
  } catch (error) {
    return authErrorResponse(error);
  }
}
```

- [ ] **Step 16: Run test, confirm it passes**

Run: `npm test -- app/api/blog/admin/posts/__tests__/route.test.js`
Expected: PASS (5 tests).

- [ ] **Step 17: Write the failing test for admin post detail/update/delete**

Create `app/api/blog/admin/posts/[id]/__tests__/route.test.js`:

```js
import { describe, it, expect, vi, beforeEach } from 'vitest';

const authenticateMock = vi.fn();
const getPostAdminByIdMock = vi.fn();
const updatePostAdminMock = vi.fn();
const deletePostAdminMock = vi.fn();

vi.mock('../../../../../../../lib/apiAuth', async () => {
  const actual = await vi.importActual('../../../../../../../lib/apiAuth');
  return { ...actual, authenticate: (...args) => authenticateMock(...args) };
});
vi.mock('../../../../../../../lib/blogPosts', () => ({
  getPostAdminById: (...args) => getPostAdminByIdMock(...args),
  updatePostAdmin: (...args) => updatePostAdminMock(...args),
  deletePostAdmin: (...args) => deletePostAdminMock(...args),
}));

import { GET, PUT, DELETE } from '../route';

function makeGetRequest() {
  return { headers: { get: () => 'Bearer good-token' } };
}
function makePutRequest(body) {
  return { headers: { get: () => 'Bearer good-token' }, json: () => Promise.resolve(body) };
}

describe('GET /api/blog/admin/posts/[id]', () => {
  beforeEach(() => {
    authenticateMock.mockReset();
    getPostAdminByIdMock.mockReset();
  });

  it('trả chi tiết khi tìm thấy', async () => {
    authenticateMock.mockResolvedValue({ user: { role: 'admin' }, supabase: {} });
    getPostAdminByIdMock.mockResolvedValue({ data: { id: 1, slug: 'bai-test' }, error: null });

    const response = await GET(makeGetRequest(), { params: Promise.resolve({ id: '1' }) });
    const body = await response.json();

    expect(body.slug).toBe('bai-test');
  });

  it('trả 404 khi không tìm thấy', async () => {
    authenticateMock.mockResolvedValue({ user: { role: 'admin' }, supabase: {} });
    getPostAdminByIdMock.mockResolvedValue({ data: null, error: null });

    const response = await GET(makeGetRequest(), { params: Promise.resolve({ id: '999' }) });

    expect(response.status).toBe(404);
  });
});

describe('PUT /api/blog/admin/posts/[id]', () => {
  beforeEach(() => {
    authenticateMock.mockReset();
    updatePostAdminMock.mockReset();
  });

  it('sửa thành công', async () => {
    authenticateMock.mockResolvedValue({ user: { role: 'admin' }, supabase: {} });
    updatePostAdminMock.mockResolvedValue({ data: { id: 1, title: 'Đổi tên' }, error: null });

    const response = await PUT(makePutRequest({
      categoryId: 1, title: 'Đổi tên', slug: 'bai-test', excerpt: 'x', content: 'x', author: 'A', status: 'published',
    }), { params: Promise.resolve({ id: '1' }) });
    const body = await response.json();

    expect(body.title).toBe('Đổi tên');
  });

  it('trả 404 khi không tìm thấy', async () => {
    authenticateMock.mockResolvedValue({ user: { role: 'admin' }, supabase: {} });
    updatePostAdminMock.mockResolvedValue({ data: null, error: null });

    const response = await PUT(makePutRequest({
      categoryId: 1, title: 'X', slug: 'x', excerpt: 'x', content: 'x', author: 'A', status: 'draft',
    }), { params: Promise.resolve({ id: '999' }) });

    expect(response.status).toBe(404);
  });
});

describe('DELETE /api/blog/admin/posts/[id]', () => {
  beforeEach(() => {
    authenticateMock.mockReset();
    deletePostAdminMock.mockReset();
  });

  it('xoá thành công, trả 204', async () => {
    authenticateMock.mockResolvedValue({ user: { role: 'admin' }, supabase: {} });
    deletePostAdminMock.mockResolvedValue({ deleted: true, error: null });

    const response = await DELETE(makeGetRequest(), { params: Promise.resolve({ id: '1' }) });

    expect(response.status).toBe(204);
  });

  it('trả 404 khi không tìm thấy', async () => {
    authenticateMock.mockResolvedValue({ user: { role: 'admin' }, supabase: {} });
    deletePostAdminMock.mockResolvedValue({ deleted: false, error: null });

    const response = await DELETE(makeGetRequest(), { params: Promise.resolve({ id: '999' }) });

    expect(response.status).toBe(404);
  });
});
```

- [ ] **Step 18: Run test, confirm it fails**

Run: `npm test -- "app/api/blog/admin/posts/\[id\]"`
Expected: FAIL with "Cannot find module '../route'".

- [ ] **Step 19: Create `app/api/blog/admin/posts/[id]/route.js`**

```js
import { NextResponse } from 'next/server';
import { authenticate, authErrorResponse, requireRole } from '../../../../../../lib/apiAuth';
import { getPostAdminById, updatePostAdmin, deletePostAdmin } from '../../../../../../lib/blogPosts';

const REQUIRED_FIELDS = ['categoryId', 'title', 'slug', 'excerpt', 'content', 'author', 'status'];

export async function GET(request, { params }) {
  try {
    const { user, supabase } = await authenticate(request);
    requireRole(user, ['admin', 'staff']);
    const { id } = await params;

    const { data, error } = await getPostAdminById(supabase, id);
    if (error) {
      return NextResponse.json({ message: 'Không tải được bài viết.' }, { status: 500 });
    }
    if (!data) {
      return NextResponse.json({ message: 'Không tìm thấy bài viết.' }, { status: 404 });
    }
    return NextResponse.json(data);
  } catch (error) {
    return authErrorResponse(error);
  }
}

export async function PUT(request, { params }) {
  try {
    const { user, supabase } = await authenticate(request);
    requireRole(user, ['admin', 'staff']);
    const { id } = await params;

    const body = await request.json();
    const missing = REQUIRED_FIELDS.some((field) => !body[field]);
    if (missing) {
      return NextResponse.json({ message: 'Thiếu thông tin bắt buộc.' }, { status: 400 });
    }

    const { data, error } = await updatePostAdmin(supabase, id, body);
    if (error) {
      if (error.code === '23505') {
        return NextResponse.json({ message: 'Slug đã tồn tại, vui lòng chọn slug khác.' }, { status: 400 });
      }
      if (error.code === '23503') {
        return NextResponse.json({ message: 'Danh mục không hợp lệ.' }, { status: 400 });
      }
      return NextResponse.json({ message: 'Không cập nhật được bài viết.' }, { status: 500 });
    }
    if (!data) {
      return NextResponse.json({ message: 'Không tìm thấy bài viết.' }, { status: 404 });
    }
    return NextResponse.json(data);
  } catch (error) {
    return authErrorResponse(error);
  }
}

export async function DELETE(request, { params }) {
  try {
    const { user, supabase } = await authenticate(request);
    requireRole(user, ['admin', 'staff']);
    const { id } = await params;

    const { deleted, error } = await deletePostAdmin(supabase, id);
    if (error) {
      return NextResponse.json({ message: 'Không xoá được bài viết.' }, { status: 500 });
    }
    if (!deleted) {
      return NextResponse.json({ message: 'Không tìm thấy bài viết.' }, { status: 404 });
    }
    return new NextResponse(null, { status: 204 });
  } catch (error) {
    return authErrorResponse(error);
  }
}
```

- [ ] **Step 20: Run test, confirm it passes**

Run: `npm test -- "app/api/blog/admin/posts/\[id\]"`
Expected: PASS (6 tests).

- [ ] **Step 21: Run the full suite, then commit**

Run: `npm test`
Expected: PASS.

```bash
git add lib/blogPosts.js lib/__tests__/blogPosts.test.js app/api/blog/posts app/api/blog/admin/posts
git commit -m "feat: add blog posts public read + admin CRUD with draft/publish workflow"
```

---

### Task 4: Frontend — `src/api/blogApi.js`

**Files:**
- Create: `Simdulich/src/api/blogApi.js`
- Create: `Simdulich/src/api/__tests__/blogApi.test.js`

**Interfaces:**
- Consumes: `apiFetch` from `Simdulich/src/api/httpClient.js` (existing, unmodified).
- Produces: `listCategoriesPublic()`, `listPostsPublic()`, `getPostBySlugPublic(slug)`,
  `listCategoriesAdmin(token)`, `createCategoryAdmin(token, body)`, `updateCategoryAdmin(token, id,
  body)`, `deleteCategoryAdmin(token, id)`, `listPostsAdmin(token, page, size)` (normalizes Spring's
  `number` → `page`, matching `searchProductsAdmin`'s precedent), `getPostAdminById(token, id)`,
  `createPostAdmin(token, body)`, `updatePostAdmin(token, id, body)`, `deletePostAdmin(token, id)` —
  Task 5/6/7 all import from this one file.

Work from: `D:\SimDuLich\Simdulich` for every step in this task.

- [ ] **Step 1: Write the failing test**

Create `Simdulich/src/api/__tests__/blogApi.test.js`:

```js
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { listPostsAdmin, createPostAdmin } from '../blogApi';

describe('listPostsAdmin', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  it('chuẩn hoá field number thành page, gửi đúng query string', async () => {
    const mockResponse = {
      ok: true,
      text: async () => JSON.stringify({
        content: [{ id: 1, title: 'Bài test', slug: 'bai-test' }],
        number: 1, size: 10, totalElements: 15, totalPages: 2,
      }),
    };
    global.fetch = vi.fn().mockResolvedValue(mockResponse);

    const result = await listPostsAdmin('tok', 1, 10);

    expect(result).toEqual({
      content: [{ id: 1, title: 'Bài test', slug: 'bai-test' }],
      page: 1, totalElements: 15, totalPages: 2,
    });
    const [url, options] = global.fetch.mock.calls[0];
    expect(url).toContain('page=1');
    expect(url).toContain('size=10');
    expect(options.headers.Authorization).toBe('Bearer tok');
  });
});

describe('createPostAdmin', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  it('gọi POST kèm body và token', async () => {
    const mockResponse = { ok: true, text: async () => JSON.stringify({ id: 7, slug: 'bai-moi' }) };
    global.fetch = vi.fn().mockResolvedValue(mockResponse);

    const body = { categoryId: 1, title: 'Bài mới', slug: 'bai-moi', excerpt: 'x', content: '<p>x</p>', author: 'A', status: 'draft' };
    const result = await createPostAdmin('tok', body);

    expect(result).toEqual({ id: 7, slug: 'bai-moi' });
    const [url, options] = global.fetch.mock.calls[0];
    expect(url).toContain('/blog/admin/posts');
    expect(options.method).toBe('POST');
    expect(JSON.parse(options.body)).toEqual(body);
  });
});
```

- [ ] **Step 2: Run test, confirm it fails**

Run: `npm test -- src/api/__tests__/blogApi.test.js`
Expected: FAIL with "Cannot find module '../blogApi'".

- [ ] **Step 3: Create `Simdulich/src/api/blogApi.js`**

```js
import { apiFetch } from './httpClient';

export function listCategoriesPublic() {
  return apiFetch('/blog/categories');
}

export function listPostsPublic() {
  return apiFetch('/blog/posts');
}

export function getPostBySlugPublic(slug) {
  return apiFetch(`/blog/posts/${slug}`);
}

export function listCategoriesAdmin(token) {
  return apiFetch('/blog/admin/categories', { token });
}

export function createCategoryAdmin(token, body) {
  return apiFetch('/blog/admin/categories', { method: 'POST', body, token });
}

export function updateCategoryAdmin(token, id, body) {
  return apiFetch(`/blog/admin/categories/${id}`, { method: 'PUT', body, token });
}

export function deleteCategoryAdmin(token, id) {
  return apiFetch(`/blog/admin/categories/${id}`, { method: 'DELETE', token });
}

// /blog/admin/posts trả Page<T> kiểu Spring (field "number") — chuẩn hoá ngay ở đây thành "page",
// giống listUsersAdmin() trong authApi.js và searchProductsAdmin() trong catalogAdminApi.js.
export async function listPostsAdmin(token, page = 0, size = 20) {
  const params = new URLSearchParams();
  params.set('page', page);
  params.set('size', size);
  const data = await apiFetch(`/blog/admin/posts?${params.toString()}`, { token });
  return { content: data.content, page: data.number, totalElements: data.totalElements, totalPages: data.totalPages };
}

export function getPostAdminById(token, id) {
  return apiFetch(`/blog/admin/posts/${id}`, { token });
}

export function createPostAdmin(token, body) {
  return apiFetch('/blog/admin/posts', { method: 'POST', body, token });
}

export function updatePostAdmin(token, id, body) {
  return apiFetch(`/blog/admin/posts/${id}`, { method: 'PUT', body, token });
}

export function deletePostAdmin(token, id) {
  return apiFetch(`/blog/admin/posts/${id}`, { method: 'DELETE', token });
}
```

- [ ] **Step 4: Run test, confirm it passes**

Run: `npm test -- src/api/__tests__/blogApi.test.js`
Expected: PASS (2 tests).

- [ ] **Step 5: Run the full suite, then commit**

Run: `npm test`
Expected: PASS.

```bash
git add src/api/blogApi.js src/api/__tests__/blogApi.test.js
git commit -m "feat: add blogApi client functions"
```

---

### Task 5: Frontend — `BlogCategoriesPage.jsx` (admin)

**Files:**
- Create: `Simdulich/src/admin/utils/slugify.js`
- Create: `Simdulich/src/admin/utils/__tests__/slugify.test.js`
- Create: `Simdulich/src/admin/pages/BlogCategoriesPage.jsx`
- Create: `Simdulich/src/admin/pages/__tests__/BlogCategoriesPage.test.jsx`
- Modify: `Simdulich/src/admin/AdminApp.jsx`
- Modify: `Simdulich/src/admin/AdminLayout.jsx`

**Interfaces:**
- Consumes: `listCategoriesAdmin`, `createCategoryAdmin`, `updateCategoryAdmin`,
  `deleteCategoryAdmin` from `src/api/blogApi.js` (Task 4).
- Produces: `slugify(text)` (pure function, exported from `src/admin/utils/slugify.js`) — Task 6's
  post editor also imports this, so its name/signature must not change.

Work from: `D:\SimDuLich\Simdulich` for every step in this task.

- [ ] **Step 1: Write the failing test for `slugify`**

Create `Simdulich/src/admin/utils/__tests__/slugify.test.js`:

```js
import { describe, it, expect } from 'vitest';
import { slugify } from '../slugify';

describe('slugify', () => {
  it('bỏ dấu tiếng Việt và thay khoảng trắng bằng gạch ngang', () => {
    expect(slugify('Cách cài đặt eSIM')).toBe('cach-cai-dat-esim');
  });

  it('xử lý chữ Đ/đ (không tự tách dấu qua NFD)', () => {
    expect(slugify('Đường truyền ổn định')).toBe('duong-truyen-on-dinh');
  });

  it('bỏ ký tự đặc biệt không phải chữ/số', () => {
    expect(slugify('So sánh: eSIM & SIM vật lý!')).toBe('so-sanh-esim-sim-vat-ly');
  });

  it('rút gọn nhiều gạch ngang liên tiếp và cắt gạch ngang ở đầu/cuối', () => {
    expect(slugify('  Nhiều   khoảng trắng  ')).toBe('nhieu-khoang-trang');
  });
});
```

- [ ] **Step 2: Run test, confirm it fails**

Run: `npm test -- src/admin/utils/__tests__/slugify.test.js`
Expected: FAIL with "Cannot find module '../slugify'".

- [ ] **Step 3: Create `Simdulich/src/admin/utils/slugify.js`**

```js
export function slugify(text) {
  return text
    .normalize('NFD')
    .replace(/[\u0300-\u036f]/g, '')
    .replace(/đ/g, 'd')
    .replace(/Đ/g, 'D')
    .toLowerCase()
    .trim()
    .replace(/[^a-z0-9\s-]/g, '')
    .replace(/\s+/g, '-')
    .replace(/-+/g, '-')
    .replace(/^-+|-+$/g, '');
}
```

- [ ] **Step 4: Run test, confirm it passes**

Run: `npm test -- src/admin/utils/__tests__/slugify.test.js`
Expected: PASS (4 tests).

- [ ] **Step 5: Write the failing test for `BlogCategoriesPage`**

Create `Simdulich/src/admin/pages/__tests__/BlogCategoriesPage.test.jsx`:

```jsx
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen, waitFor } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { AuthProvider } from '../../../context/AuthContext';
import BlogCategoriesPage from '../BlogCategoriesPage';
import * as blogApi from '../../../api/blogApi';

vi.mock('../../../api/blogApi');

const CATEGORY = { id: 1, name: 'Hướng dẫn cài đặt', slug: 'guides' };

function renderPage() {
  return render(<AuthProvider><BlogCategoriesPage /></AuthProvider>);
}

describe('BlogCategoriesPage', () => {
  beforeEach(() => {
    localStorage.clear();
    localStorage.setItem('simdulich_auth', JSON.stringify({ accessToken: 'tok', refreshToken: 'r', user: { role: 'admin' } }));
    blogApi.listCategoriesAdmin.mockResolvedValue([CATEGORY]);
    blogApi.createCategoryAdmin.mockResolvedValue({ id: 2, name: 'Mới', slug: 'moi' });
  });

  it('hiển thị danh mục thật từ API', async () => {
    renderPage();
    await waitFor(() => expect(screen.getAllByTestId('blog-category-row').length).toBe(1));
    expect(screen.getByText('Hướng dẫn cài đặt')).toBeInTheDocument();
  });

  it('thêm danh mục mới tự sinh slug từ tên rồi gọi createCategoryAdmin', async () => {
    renderPage();
    await waitFor(() => expect(screen.getAllByTestId('blog-category-row').length).toBe(1));
    await userEvent.click(screen.getByRole('button', { name: /Thêm danh mục/i }));
    await userEvent.type(screen.getByLabelText('Tên danh mục'), 'Đánh giá sản phẩm');
    blogApi.listCategoriesAdmin.mockResolvedValue([CATEGORY, { id: 2, name: 'Đánh giá sản phẩm', slug: 'danh-gia-san-pham' }]);
    await userEvent.click(screen.getByRole('button', { name: /Lưu danh mục/i }));
    expect(blogApi.createCategoryAdmin).toHaveBeenCalledWith('tok', { name: 'Đánh giá sản phẩm', slug: 'danh-gia-san-pham' });
    await waitFor(() => expect(screen.getAllByTestId('blog-category-row').length).toBe(2));
  });
});
```

- [ ] **Step 6: Run test, confirm it fails**

Run: `npm test -- src/admin/pages/__tests__/BlogCategoriesPage.test.jsx`
Expected: FAIL with "Cannot find module '../BlogCategoriesPage'".

- [ ] **Step 7: Create `Simdulich/src/admin/pages/BlogCategoriesPage.jsx`**

```jsx
import { useState, useEffect, useCallback } from 'react';
import { useAuth } from '../../context/AuthContext';
import { listCategoriesAdmin, createCategoryAdmin, updateCategoryAdmin, deleteCategoryAdmin } from '../../api/blogApi';
import { slugify } from '../utils/slugify';
import { Plus, Pencil, Trash2, X } from 'lucide-react';

const EMPTY = { name: '', slug: '' };

export default function BlogCategoriesPage() {
  const { accessToken } = useAuth();
  const [categories, setCategories] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState('');
  const [form, setForm] = useState(null);

  const load = useCallback(() => {
    if (!accessToken) return;
    setLoading(true);
    setError('');
    listCategoriesAdmin(accessToken)
      .then(setCategories)
      .catch((err) => setError(err.message || 'Không tải được danh mục.'))
      .finally(() => setLoading(false));
  }, [accessToken]);

  useEffect(() => { load(); }, [load]);

  const submit = async (e) => {
    e.preventDefault();
    const body = { name: form.name, slug: form.slug || slugify(form.name) };
    try {
      if (form.id) {
        await updateCategoryAdmin(accessToken, form.id, body);
      } else {
        await createCategoryAdmin(accessToken, body);
      }
      setForm(null);
      load();
    } catch (err) {
      setError(err.message || 'Lưu danh mục thất bại.');
    }
  };

  const remove = async (id) => {
    if (!window.confirm('Xóa danh mục này?')) return;
    try {
      await deleteCategoryAdmin(accessToken, id);
      load();
    } catch (err) {
      setError(err.message || 'Xóa danh mục thất bại.');
    }
  };

  return (
    <div>
      <div className="flex items-center justify-between mb-6">
        <h1 className="text-2xl font-extrabold text-slate-900">Danh mục Blog</h1>
        <button onClick={() => setForm({ ...EMPTY })} className="px-4 py-2.5 bg-brand text-white font-bold rounded-full hover:bg-brand-hover flex items-center gap-1.5 text-sm">
          <Plus className="h-4 w-4" /> Thêm danh mục
        </button>
      </div>

      {error && (
        <div className="bg-rose-50 border border-rose-200 text-rose-700 text-sm rounded-xl px-4 py-3 mb-4">{error}</div>
      )}
      {loading && <p className="text-slate-400 text-sm">Đang tải...</p>}

      <div className="bg-white border border-slate-200 rounded-2xl overflow-hidden divide-y divide-slate-50">
        {!loading && categories.map((c) => (
          <div key={c.id} data-testid="blog-category-row" className="flex items-center justify-between px-4 py-3">
            <div>
              <p className="font-semibold text-slate-800">{c.name}</p>
              <p className="text-xs text-slate-400">/{c.slug}</p>
            </div>
            <div className="flex gap-3">
              <button onClick={() => setForm({ ...c })} className="text-xs font-bold text-brand flex items-center gap-1"><Pencil className="h-3.5 w-3.5" /> Sửa</button>
              <button onClick={() => remove(c.id)} className="text-xs font-bold text-rose-600 flex items-center gap-1"><Trash2 className="h-3.5 w-3.5" /> Xóa</button>
            </div>
          </div>
        ))}
      </div>

      {form && (
        <div className="fixed inset-0 bg-black/30 flex justify-end z-50" onClick={() => setForm(null)}>
          <form onSubmit={submit} onClick={(e) => e.stopPropagation()} className="w-full max-w-sm bg-white h-full p-6 space-y-4">
            <div className="flex items-center justify-between">
              <h2 className="font-bold text-lg text-slate-900">{form.id ? 'Sửa danh mục' : 'Thêm danh mục'}</h2>
              <button type="button" onClick={() => setForm(null)} className="p-1 text-slate-400"><X className="h-5 w-5" /></button>
            </div>
            <label className="block"><span className="text-xs font-bold text-slate-500 uppercase block mb-1">Tên danh mục</span>
              <input aria-label="Tên danh mục" value={form.name} onChange={(e) => setForm((f) => ({ ...f, name: e.target.value }))} className="w-full bg-slate-50 border border-slate-200 rounded-xl px-3 py-2 text-sm" /></label>
            <label className="block"><span className="text-xs font-bold text-slate-500 uppercase block mb-1">Slug (URL)</span>
              <input value={form.slug} onChange={(e) => setForm((f) => ({ ...f, slug: e.target.value }))} className="w-full bg-slate-50 border border-slate-200 rounded-xl px-3 py-2 text-sm" placeholder="tự sinh nếu để trống" /></label>
            <button type="submit" className="w-full py-2.5 bg-brand text-white font-bold rounded-full hover:bg-brand-hover">Lưu danh mục</button>
          </form>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 8: Run test, confirm it passes**

Run: `npm test -- src/admin/pages/__tests__/BlogCategoriesPage.test.jsx`
Expected: PASS (2 tests).

- [ ] **Step 9: Register the new page — modify `AdminApp.jsx` and `AdminLayout.jsx`**

In `Simdulich/src/admin/AdminApp.jsx`, add the import and route branch:

```js
import BlogCategoriesPage from './pages/BlogCategoriesPage';
```

```js
  else if (route === 'blog-categories') page = <BlogCategoriesPage />;
```//NOTE Insert this `else if` line among the other `else if (route === ...)` lines, right before
the final `else page = <Placeholder .../>` line.

In `Simdulich/src/admin/AdminLayout.jsx`, find the `NAV` array's group containing `categories`
(`{ key: 'categories', label: 'Danh mục quốc gia', icon: Globe }`) and add a new group after it:

```js
  { group: 'Nội dung', items: [{ key: 'blog-categories', label: 'Danh mục Blog', icon: Tag }] },
```

Add `Tag` to the `lucide-react` import at the top of the file (alongside the other icon imports
already there, e.g. `Globe`, `Ticket`).

- [ ] **Step 10: Run the full suite, then commit**

Run: `npm test`
Expected: PASS.

```bash
git add src/admin/utils/slugify.js src/admin/utils/__tests__/slugify.test.js src/admin/pages/BlogCategoriesPage.jsx src/admin/pages/__tests__/BlogCategoriesPage.test.jsx src/admin/AdminApp.jsx src/admin/AdminLayout.jsx
git commit -m "feat: add admin blog categories page"
```

---

### Task 6: Frontend — Blog posts admin pages (list + TipTap editor)

**Files:**
- Create: `Simdulich/src/admin/pages/BlogPostsPage.jsx`
- Create: `Simdulich/src/admin/pages/__tests__/BlogPostsPage.test.jsx`
- Create: `Simdulich/src/admin/pages/BlogPostEditorPage.jsx`
- Create: `Simdulich/src/admin/pages/__tests__/BlogPostEditorPage.test.jsx`
- Modify: `Simdulich/src/admin/AdminApp.jsx`
- Modify: `Simdulich/src/admin/AdminLayout.jsx`
- Modify: `Simdulich/package.json` (via `npm install`, not hand-edited)

**Interfaces:**
- Consumes: `listPostsAdmin`, `getPostAdminById`, `createPostAdmin`, `updatePostAdmin`,
  `deletePostAdmin`, `listCategoriesAdmin` from `src/api/blogApi.js` (Task 4); `slugify` from
  `src/admin/utils/slugify.js` (Task 5).
- Produces: nothing new — last consumer of Task 4/5's interfaces on the admin side.

Work from: `D:\SimDuLich\Simdulich` for every step in this task.

- [ ] **Step 1: Install TipTap**

```bash
npm install @tiptap/react @tiptap/pm @tiptap/starter-kit
```

- [ ] **Step 2: Write the failing test for `BlogPostsPage`**

Create `Simdulich/src/admin/pages/__tests__/BlogPostsPage.test.jsx`:

```jsx
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen, waitFor } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { AuthProvider } from '../../../context/AuthContext';
import BlogPostsPage from '../BlogPostsPage';
import * as blogApi from '../../../api/blogApi';

vi.mock('../../../api/blogApi');

const POST = { id: 1, title: 'Bài test', slug: 'bai-test', status: 'published', categoryName: 'Hướng dẫn cài đặt', createdAt: '2026-06-01T00:00:00Z' };

function renderPage(props = {}) {
  return render(<AuthProvider><BlogPostsPage {...props} /></AuthProvider>);
}

describe('BlogPostsPage', () => {
  beforeEach(() => {
    localStorage.clear();
    localStorage.setItem('simdulich_auth', JSON.stringify({ accessToken: 'tok', refreshToken: 'r', user: { role: 'admin' } }));
    blogApi.listPostsAdmin.mockResolvedValue({ content: [POST], page: 0, totalElements: 1, totalPages: 1 });
    blogApi.deletePostAdmin.mockResolvedValue();
  });

  it('hiển thị bài viết thật từ API', async () => {
    renderPage();
    await waitFor(() => expect(screen.getAllByTestId('blog-post-row').length).toBe(1));
    expect(screen.getByText('Bài test')).toBeInTheDocument();
  });

  it('bấm Viết bài mới gọi onNewPost', async () => {
    const onNewPost = vi.fn();
    renderPage({ onNewPost, onEditPost: vi.fn() });
    await userEvent.click(screen.getByRole('button', { name: /Viết bài mới/i }));
    expect(onNewPost).toHaveBeenCalled();
  });

  it('bấm Xoá gọi deletePostAdmin rồi tải lại danh sách', async () => {
    renderPage({ onNewPost: vi.fn(), onEditPost: vi.fn() });
    await waitFor(() => expect(screen.getAllByTestId('blog-post-row').length).toBe(1));
    vi.spyOn(window, 'confirm').mockReturnValue(true);
    blogApi.listPostsAdmin.mockResolvedValue({ content: [], page: 0, totalElements: 0, totalPages: 0 });
    await userEvent.click(screen.getByRole('button', { name: /Xoá/i }));
    expect(blogApi.deletePostAdmin).toHaveBeenCalledWith('tok', 1);
    await waitFor(() => expect(screen.queryAllByTestId('blog-post-row').length).toBe(0));
  });
});
```

- [ ] **Step 3: Run test, confirm it fails**

Run: `npm test -- src/admin/pages/__tests__/BlogPostsPage.test.jsx`
Expected: FAIL with "Cannot find module '../BlogPostsPage'".

- [ ] **Step 4: Create `Simdulich/src/admin/pages/BlogPostsPage.jsx`**

```jsx
import { useState, useEffect, useCallback } from 'react';
import { useAuth } from '../../context/AuthContext';
import { listPostsAdmin, deletePostAdmin } from '../../api/blogApi';
import { usePagination } from '../components/usePagination';
import Pagination from '../components/Pagination';
import { Plus, Pencil, Trash2 } from 'lucide-react';

const STATUS_LABEL = { draft: 'Nháp', published: 'Đã đăng' };
const STATUS_CLS = { draft: 'bg-slate-100 text-slate-500', published: 'bg-emerald-50 text-emerald-600' };

export default function BlogPostsPage({ onNewPost, onEditPost }) {
  const { accessToken } = useAuth();
  const [posts, setPosts] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState('');
  const [page, setPage] = useState(1);
  const [totalPages, setTotalPages] = useState(1);
  const [total, setTotal] = useState(0);

  const load = useCallback(() => {
    if (!accessToken) return;
    setLoading(true);
    setError('');
    listPostsAdmin(accessToken, page - 1, 10)
      .then((res) => { setPosts(res.content); setTotalPages(Math.max(1, res.totalPages)); setTotal(res.totalElements); })
      .catch((err) => setError(err.message || 'Không tải được danh sách bài viết.'))
      .finally(() => setLoading(false));
  }, [accessToken, page]);

  useEffect(() => { load(); }, [load]);

  const remove = async (id) => {
    if (!window.confirm('Xóa bài viết này?')) return;
    try {
      await deletePostAdmin(accessToken, id);
      load();
    } catch (err) {
      setError(err.message || 'Xóa bài viết thất bại.');
    }
  };

  return (
    <div>
      <div className="flex items-center justify-between mb-6">
        <h1 className="text-2xl font-extrabold text-slate-900">Bài viết Blog</h1>
        <button onClick={() => onNewPost && onNewPost()} className="px-4 py-2.5 bg-brand text-white font-bold rounded-full hover:bg-brand-hover flex items-center gap-1.5 text-sm">
          <Plus className="h-4 w-4" /> Viết bài mới
        </button>
      </div>

      {error && (
        <div className="bg-rose-50 border border-rose-200 text-rose-700 text-sm rounded-xl px-4 py-3 mb-4">{error}</div>
      )}

      <div className="bg-white border border-slate-200 rounded-2xl overflow-hidden">
        <table className="w-full text-sm">
          <thead>
            <tr className="text-[11px] uppercase tracking-wider text-slate-400 border-b border-slate-100">
              <th className="text-left font-bold px-4 py-3">Tiêu đề</th>
              <th className="text-left font-bold px-4 py-3">Danh mục</th>
              <th className="text-left font-bold px-4 py-3">Trạng thái</th>
              <th className="px-4 py-3"></th>
            </tr>
          </thead>
          <tbody>
            {loading && (
              <tr><td colSpan={4} className="px-4 py-10 text-center text-slate-400">Đang tải...</td></tr>
            )}
            {!loading && posts.map((p) => (
              <tr key={p.id} data-testid="blog-post-row" className="border-b border-slate-50 hover:bg-slate-50">
                <td className="px-4 py-3 font-semibold text-slate-700">{p.title}</td>
                <td className="px-4 py-3 text-slate-500">{p.categoryName}</td>
                <td className="px-4 py-3"><span className={`px-2 py-0.5 rounded-full text-xs font-bold ${STATUS_CLS[p.status]}`}>{STATUS_LABEL[p.status]}</span></td>
                <td className="px-4 py-3 text-right whitespace-nowrap">
                  <button onClick={() => onEditPost && onEditPost(p.id)} aria-label="Sửa" className="p-2 text-slate-400 hover:text-brand"><Pencil className="h-4 w-4" /></button>
                  <button onClick={() => remove(p.id)} aria-label="Xoá" className="p-2 text-slate-400 hover:text-rose-600">Xoá <Trash2 className="h-4 w-4 inline" /></button>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
        <Pagination page={page} totalPages={totalPages} total={total} pageSize={10} onPage={setPage} />
      </div>
    </div>
  );
}
```

Note: the "Xoá" button's accessible name in the test is matched via `/Xoá/i` (a `getByRole('button',
{name: ...})` matches on the button's full text content, so the literal text `Xoá` next to the icon
is enough — no separate `aria-label` needed on that specific button, unlike "Sửa" which uses
`aria-label="Sửa"` since it has no visible text, only an icon).

- [ ] **Step 5: Run test, confirm it passes**

Run: `npm test -- src/admin/pages/__tests__/BlogPostsPage.test.jsx`
Expected: PASS (3 tests).

- [ ] **Step 6: Write the failing test for `BlogPostEditorPage`**

Create `Simdulich/src/admin/pages/__tests__/BlogPostEditorPage.test.jsx`:

```jsx
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen, waitFor } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { AuthProvider } from '../../../context/AuthContext';
import BlogPostEditorPage from '../BlogPostEditorPage';
import * as blogApi from '../../../api/blogApi';

vi.mock('../../../api/blogApi');

const CATEGORY = { id: 1, name: 'Hướng dẫn cài đặt', slug: 'guides' };
const POST = {
  id: 5, categoryId: 1, title: 'Bài cũ', slug: 'bai-cu', excerpt: 'Mô tả', content: '<p>Nội dung cũ</p>',
  imageUrl: '/images/x.png', author: 'A', status: 'draft',
};

function renderPage(props) {
  return render(<AuthProvider><BlogPostEditorPage onDone={vi.fn()} {...props} /></AuthProvider>);
}

describe('BlogPostEditorPage', () => {
  beforeEach(() => {
    localStorage.clear();
    localStorage.setItem('simdulich_auth', JSON.stringify({ accessToken: 'tok', refreshToken: 'r', user: { role: 'admin' } }));
    blogApi.listCategoriesAdmin.mockResolvedValue([CATEGORY]);
    blogApi.getPostAdminById.mockResolvedValue(POST);
    blogApi.createPostAdmin.mockResolvedValue({ id: 9, slug: 'bai-moi' });
    blogApi.updatePostAdmin.mockResolvedValue({ id: 5, slug: 'bai-cu' });
  });

  it('tạo bài mới: tự sinh slug từ tiêu đề, gọi createPostAdmin với status draft khi bấm Lưu nháp', async () => {
    renderPage({ postId: null });
    await waitFor(() => expect(screen.getByLabelText('Tiêu đề')).toBeInTheDocument());
    await userEvent.type(screen.getByLabelText('Tiêu đề'), 'Bài mới của tôi');
    await userEvent.click(screen.getByRole('button', { name: /Lưu nháp/i }));

    expect(blogApi.createPostAdmin).toHaveBeenCalledWith('tok', expect.objectContaining({
      title: 'Bài mới của tôi', slug: 'bai-moi-cua-toi', status: 'draft',
    }));
  });

  it('sửa bài có sẵn: nạp đúng dữ liệu ban đầu, bấm Đăng bài gọi updatePostAdmin với status published', async () => {
    renderPage({ postId: 5 });
    await waitFor(() => expect(screen.getByLabelText('Tiêu đề')).toHaveValue('Bài cũ'));

    await userEvent.click(screen.getByRole('button', { name: /Đăng bài/i }));

    expect(blogApi.updatePostAdmin).toHaveBeenCalledWith('tok', 5, expect.objectContaining({ status: 'published' }));
  });
});
```

- [ ] **Step 7: Run test, confirm it fails**

Run: `npm test -- src/admin/pages/__tests__/BlogPostEditorPage.test.jsx`
Expected: FAIL with "Cannot find module '../BlogPostEditorPage'".

- [ ] **Step 8: Create `Simdulich/src/admin/pages/BlogPostEditorPage.jsx`**

```jsx
import { useState, useEffect, useCallback } from 'react';
import { useEditor, EditorContent } from '@tiptap/react';
import StarterKit from '@tiptap/starter-kit';
import { useAuth } from '../../context/AuthContext';
import { listCategoriesAdmin, getPostAdminById, createPostAdmin, updatePostAdmin } from '../../api/blogApi';
import { slugify } from '../utils/slugify';

const EMPTY = { categoryId: '', title: '', slug: '', excerpt: '', content: '', imageUrl: '', author: '' };

function ContentEditor({ value, onChange }) {
  const editor = useEditor({
    extensions: [StarterKit],
    content: value || '',
    onUpdate: ({ editor: e }) => onChange(e.getHTML()),
  });

  useEffect(() => {
    if (editor && value !== editor.getHTML()) {
      editor.commands.setContent(value || '', false);
    }
  }, [editor, value]);

  return (
    <div className="border border-slate-200 rounded-xl">
      <EditorContent editor={editor} className="prose max-w-none p-4 min-h-[300px]" />
    </div>
  );
}

export default function BlogPostEditorPage({ postId, onDone }) {
  const { accessToken } = useAuth();
  const [categories, setCategories] = useState([]);
  const [form, setForm] = useState({ ...EMPTY });
  const [slugTouched, setSlugTouched] = useState(false);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState('');

  useEffect(() => {
    if (!accessToken) return;
    setLoading(true);
    setError('');
    Promise.all([
      listCategoriesAdmin(accessToken),
      postId ? getPostAdminById(accessToken, postId) : Promise.resolve(null),
    ])
      .then(([cats, post]) => {
        setCategories(cats);
        if (post) {
          setForm({
            categoryId: post.categoryId, title: post.title, slug: post.slug, excerpt: post.excerpt,
            content: post.content, imageUrl: post.imageUrl || '', author: post.author,
          });
          setSlugTouched(true);
        }
      })
      .catch((err) => setError(err.message || 'Không tải được dữ liệu.'))
      .finally(() => setLoading(false));
  }, [accessToken, postId]);

  const handleTitleChange = useCallback((title) => {
    setForm((f) => ({ ...f, title, slug: slugTouched ? f.slug : slugify(title) }));
  }, [slugTouched]);

  const save = async (status) => {
    setError('');
    const body = { ...form, categoryId: Number(form.categoryId), status };
    try {
      if (postId) {
        await updatePostAdmin(accessToken, postId, body);
      } else {
        await createPostAdmin(accessToken, body);
      }
      onDone && onDone();
    } catch (err) {
      setError(err.message || 'Lưu bài viết thất bại.');
    }
  };

  if (loading) return <p className="text-slate-400 text-sm">Đang tải...</p>;

  return (
    <div className="max-w-3xl space-y-4">
      <h1 className="text-2xl font-extrabold text-slate-900">{postId ? 'Sửa bài viết' : 'Viết bài mới'}</h1>

      {error && (
        <div className="bg-rose-50 border border-rose-200 text-rose-700 text-sm rounded-xl px-4 py-3">{error}</div>
      )}

      <label className="block"><span className="text-xs font-bold text-slate-500 uppercase block mb-1">Tiêu đề</span>
        <input aria-label="Tiêu đề" value={form.title} onChange={(e) => handleTitleChange(e.target.value)} className="w-full bg-slate-50 border border-slate-200 rounded-xl px-3 py-2 text-sm" /></label>

      <label className="block"><span className="text-xs font-bold text-slate-500 uppercase block mb-1">Slug (URL)</span>
        <input aria-label="Slug" value={form.slug} onChange={(e) => { setSlugTouched(true); setForm((f) => ({ ...f, slug: e.target.value })); }} className="w-full bg-slate-50 border border-slate-200 rounded-xl px-3 py-2 text-sm" /></label>

      <label className="block"><span className="text-xs font-bold text-slate-500 uppercase block mb-1">Danh mục</span>
        <select aria-label="Danh mục" value={form.categoryId} onChange={(e) => setForm((f) => ({ ...f, categoryId: e.target.value }))} className="w-full bg-slate-50 border border-slate-200 rounded-xl px-3 py-2 text-sm">
          <option value="">-- Chọn danh mục --</option>
          {categories.map((c) => <option key={c.id} value={c.id}>{c.name}</option>)}
        </select></label>

      <label className="block"><span className="text-xs font-bold text-slate-500 uppercase block mb-1">URL ảnh đại diện</span>
        <input aria-label="URL ảnh đại diện" value={form.imageUrl} onChange={(e) => setForm((f) => ({ ...f, imageUrl: e.target.value }))} className="w-full bg-slate-50 border border-slate-200 rounded-xl px-3 py-2 text-sm" /></label>

      <label className="block"><span className="text-xs font-bold text-slate-500 uppercase block mb-1">Mô tả ngắn</span>
        <textarea aria-label="Mô tả ngắn" value={form.excerpt} onChange={(e) => setForm((f) => ({ ...f, excerpt: e.target.value }))} className="w-full bg-slate-50 border border-slate-200 rounded-xl px-3 py-2 text-sm" rows={2} /></label>

      <label className="block"><span className="text-xs font-bold text-slate-500 uppercase block mb-1">Tác giả</span>
        <input aria-label="Tác giả" value={form.author} onChange={(e) => setForm((f) => ({ ...f, author: e.target.value }))} className="w-full bg-slate-50 border border-slate-200 rounded-xl px-3 py-2 text-sm" /></label>

      <div>
        <span className="text-xs font-bold text-slate-500 uppercase block mb-1">Nội dung</span>
        <ContentEditor value={form.content} onChange={(content) => setForm((f) => ({ ...f, content }))} />
      </div>

      <div className="flex gap-3">
        <button type="button" onClick={() => save('draft')} className="flex-1 py-2.5 bg-slate-100 text-slate-700 font-bold rounded-full hover:bg-slate-200">Lưu nháp</button>
        <button type="button" onClick={() => save('published')} className="flex-1 py-2.5 bg-brand text-white font-bold rounded-full hover:bg-brand-hover">Đăng bài</button>
      </div>
    </div>
  );
}
```

- [ ] **Step 9: Run test, confirm it passes**

Run: `npm test -- src/admin/pages/__tests__/BlogPostEditorPage.test.jsx`
Expected: PASS (2 tests).

- [ ] **Step 10: Register both pages — modify `AdminApp.jsx` and `AdminLayout.jsx`**

In `Simdulich/src/admin/AdminApp.jsx`, add imports and state for navigating between the list and
editor (mirrors the existing `orders`/`order-detail` pattern already in this file):

```js
import BlogPostsPage from './pages/BlogPostsPage';
import BlogPostEditorPage from './pages/BlogPostEditorPage';
```

Add a new piece of state alongside `selectedOrderId`:

```js
  const [editingPostId, setEditingPostId] = useState(undefined); // undefined = not editing, null = new post, number = editing that id
```

Add route branches (alongside the existing `orders`/`order-detail` branches):

```js
  else if (route === 'blog-posts') page = <BlogPostsPage onNewPost={() => { setEditingPostId(null); setRoute('blog-post-editor'); }} onEditPost={(id) => { setEditingPostId(id); setRoute('blog-post-editor'); }} />;
  else if (route === 'blog-post-editor') page = <BlogPostEditorPage postId={editingPostId} onDone={() => setRoute('blog-posts')} />;
```

Update the `navRoute` line (so the sidebar highlights "Bài viết Blog" while the editor is open,
mirroring how `order-detail` maps back to `orders`):

```js
  const navRoute = route === 'order-detail' ? 'orders' : route === 'blog-post-editor' ? 'blog-posts' : route;
```

In `Simdulich/src/admin/AdminLayout.jsx`, add `'blog-posts'` to the same `'Nội dung'` group created
in Task 5, before `'blog-categories'`:

```js
  { group: 'Nội dung', items: [
    { key: 'blog-posts', label: 'Bài viết Blog', icon: Newspaper },
    { key: 'blog-categories', label: 'Danh mục Blog', icon: Tag },
  ] },
```

(This replaces the single-item group Task 5 added — Task 6 supersedes it with both items in one
group.) Add `Newspaper` to the `lucide-react` import alongside `Tag`.

- [ ] **Step 11: Run the full suite, then commit**

Run: `npm test`
Expected: PASS.

```bash
git add package.json package-lock.json src/admin/pages/BlogPostsPage.jsx src/admin/pages/__tests__/BlogPostsPage.test.jsx src/admin/pages/BlogPostEditorPage.jsx src/admin/pages/__tests__/BlogPostEditorPage.test.jsx src/admin/AdminApp.jsx src/admin/AdminLayout.jsx
git commit -m "feat: add admin blog posts list + TipTap editor pages"
```

---

### Task 7: Frontend — public page rewiring + slug routing

**Files:**
- Modify: `Simdulich/src/user/pages/BlogPage.jsx`
- Modify: `Simdulich/src/user/pages/__tests__/BlogPage.test.jsx` (create if it doesn't exist yet —
  check first)
- Modify: `Simdulich/src/user/components/Blog.jsx`
- Modify: `Simdulich/src/user/components/__tests__/Blog.test.jsx` (create if it doesn't exist yet —
  check first)
- Modify: `Simdulich/src/App.jsx`
- Modify: `Simdulich/src/data/defaultSiteContent.js` (remove `blogPosts` field)

**Interfaces:**
- Consumes: `listPostsPublic`, `getPostBySlugPublic`, `listCategoriesPublic` from
  `src/api/blogApi.js` (Task 4).
- Produces: nothing new — this is the last task before manual verification.

Work from: `D:\SimDuLich\Simdulich` for every step in this task.

- [ ] **Step 1: Check for existing tests first**

Run: `ls src/user/pages/__tests__/BlogPage.test.jsx src/user/components/__tests__/Blog.test.jsx 2>&1`

If either file exists, read it fully before proceeding — you'll be rewriting its mocks from
`SiteContentContext` to `blogApi`, not creating a new file from scratch. If a file doesn't exist,
you'll create it fresh with the tests given below.

- [ ] **Step 2: Write/update the failing test for `Blog.jsx` (homepage teaser)**

Create or replace `Simdulich/src/user/components/__tests__/Blog.test.jsx`:

```jsx
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen, waitFor } from '@testing-library/react';
import Blog from '../Blog';
import * as blogApi from '../../../api/blogApi';

vi.mock('../../../api/blogApi');

describe('Blog (homepage teaser)', () => {
  beforeEach(() => {
    blogApi.listPostsPublic.mockResolvedValue([
      { id: 1, title: 'Bài 1', slug: 'bai-1', excerpt: 'Mô tả 1', imageUrl: '/images/1.png', categoryName: 'Guides', publishedAt: '2026-06-17T00:00:00Z' },
    ]);
  });

  it('hiển thị bài viết thật từ API', async () => {
    render(<Blog onReadMore={vi.fn()} />);
    await waitFor(() => expect(screen.getByText('Bài 1')).toBeInTheDocument());
  });
});
```

- [ ] **Step 3: Run test, confirm it fails**

Run: `npm test -- src/user/components/__tests__/Blog.test.jsx`
Expected: FAIL — `Blog.jsx` still reads from `SiteContentContext`, not `blogApi`, so `listPostsPublic`
is never called and the mocked post never renders.

- [ ] **Step 4: Rewrite `Simdulich/src/user/components/Blog.jsx`**

Replace the file's `useSiteContent` usage with a `useEffect` fetch, keeping every other line
(markup/classes) identical to the current file:

```jsx
import { useState, useEffect } from 'react';
import { listPostsPublic } from '../../api/blogApi';

export default function Blog({ onReadMore }) {
  const [allPosts, setAllPosts] = useState([]);

  useEffect(() => {
    listPostsPublic().then(setAllPosts).catch(() => setAllPosts([]));
  }, []);

  const posts = allPosts.slice(0, 6);

  const handlePostClick = (e) => {
    e.preventDefault();
    if (onReadMore) {
      onReadMore();
    }
  };

  return (
    <section id="blog" className="mb-[80px] w-full py-6">
      <div className="container mx-auto px-4 max-w-[1232px]">
        <div className="text-center mb-12">
          <h2 className="m3-section-title text-[#233475]">
            Tin tức & Kinh nghiệm nổi bật
          </h2>
          <p className="text-slate-500 mt-2">Cập nhật xu hướng công nghệ du lịch và cẩm nang phượt hữu ích</p>
        </div>

        {posts.length > 0 ? (
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 text-left">
            {posts.map((post) => (
              <article
                key={post.id}
                className="bg-white rounded-3xl overflow-hidden border border-slate-100 shadow-sm hover:shadow-xl hover:border-primary/10 transition-all duration-300 group flex flex-col h-full text-left"
              >
                <div className="relative overflow-hidden aspect-video bg-slate-100 cursor-pointer" onClick={handlePostClick}>
                  <img
                    src={post.imageUrl}
                    alt={post.title}
                    className="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"
                  />
                  <span className="absolute top-4 left-4 bg-white/90 backdrop-blur-md text-primary font-bold text-[11px] px-3 py-1 rounded-full uppercase tracking-wider shadow-sm">
                    {post.categoryName}
                  </span>
                </div>

                <div className="p-6 flex flex-col justify-between flex-1">
                  <div>
                    <h3 className="text-lg font-bold text-slate-800 group-hover:text-primary transition-colors leading-snug line-clamp-2 cursor-pointer" onClick={handlePostClick}>
                      {post.title}
                    </h3>
                    <p className="text-sm text-slate-500 mt-3 leading-relaxed line-clamp-3">
                      {post.excerpt}
                    </p>
                  </div>

                  <div className="mt-6 pt-4 border-t border-slate-50 flex items-center justify-between">
                    <span className="text-xs text-slate-400 font-semibold">{new Date(post.publishedAt).toLocaleDateString('vi-VN')}</span>
                    <a href={`/blog/${post.slug}`} onClick={handlePostClick} className="text-xs text-primary font-bold hover:underline flex items-center gap-1">
                      Đọc tiếp →
                    </a>
                  </div>
                </div>
              </article>
            ))}
          </div>
        ) : (
          <div className="text-center py-12 text-slate-400">
            Chưa có bài viết nào nổi bật.
          </div>
        )}

        <div className="mt-10 flex justify-center">
          <button
            onClick={() => onReadMore && onReadMore()}
            className="inline-flex items-center justify-center rounded-full bg-primary px-8 py-3.5 text-base font-bold text-white shadow-md shadow-primary/25 transition-colors hover:bg-primary/90"
          >
            Xem tất cả bài viết
          </button>
        </div>
      </div>
    </section>
  );
}
```

- [ ] **Step 5: Run test, confirm it passes**

Run: `npm test -- src/user/components/__tests__/Blog.test.jsx`
Expected: PASS (1 test).

- [ ] **Step 6: Write/update the failing test for `BlogPage.jsx`**

Create or replace `Simdulich/src/user/pages/__tests__/BlogPage.test.jsx`:

```jsx
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen, waitFor } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import BlogPage from '../BlogPage';
import * as blogApi from '../../../api/blogApi';

vi.mock('../../../api/blogApi');

const POST = {
  id: 1, title: 'Bài test', slug: 'bai-test', excerpt: 'Mô tả', content: '<p>Nội dung</p>',
  imageUrl: '/images/x.png', author: 'A', categoryName: 'Guides', publishedAt: '2026-06-17T00:00:00Z',
};

describe('BlogPage', () => {
  beforeEach(() => {
    blogApi.listPostsPublic.mockResolvedValue([POST]);
    blogApi.getPostBySlugPublic.mockResolvedValue(POST);
  });

  it('hiển thị danh sách bài viết thật từ API', async () => {
    render(<BlogPage />);
    await waitFor(() => expect(screen.getByText('Bài test')).toBeInTheDocument());
  });

  it('bấm vào 1 bài mở đúng nội dung, gọi pushState với đúng slug', async () => {
    const pushStateSpy = vi.spyOn(window.history, 'pushState');
    render(<BlogPage />);
    await waitFor(() => expect(screen.getByText('Bài test')).toBeInTheDocument());
    await userEvent.click(screen.getByText('Bài test'));
    expect(pushStateSpy).toHaveBeenCalledWith(null, '', '/blog/bai-test');
    await waitFor(() => expect(screen.getByText('Nội dung')).toBeInTheDocument());
  });

  it('nhận initialSlug qua prop thì tự fetch và hiện đúng bài đó ngay khi vào trang', async () => {
    render(<BlogPage initialSlug="bai-test" />);
    await waitFor(() => expect(blogApi.getPostBySlugPublic).toHaveBeenCalledWith('bai-test'));
    await waitFor(() => expect(screen.getByText('Nội dung')).toBeInTheDocument());
  });
});
```

- [ ] **Step 7: Run test, confirm it fails**

Run: `npm test -- src/user/pages/__tests__/BlogPage.test.jsx`
Expected: FAIL — `BlogPage.jsx` still reads `content.blogPosts` from `SiteContentContext` and has
no `initialSlug` prop/pushState behavior yet.

- [ ] **Step 8: Rewrite `Simdulich/src/user/pages/BlogPage.jsx`**

Replace the file's data source and add slug-based navigation, keeping the JSX/markup for both the
list grid and the single-article view otherwise identical to the current file (only the data
source, category list source, and the two navigation handlers change):

```jsx
import { useState, useEffect } from 'react';
import { Search, ChevronRight, ArrowLeft, Calendar, User, Clock } from 'lucide-react';
import { listPostsPublic, getPostBySlugPublic, listCategoriesPublic } from '../../api/blogApi';

export default function BlogPage({ initialSlug }) {
  const [allPosts, setAllPosts] = useState([]);
  const [categories, setCategories] = useState([{ id: 'All', name: 'Tất cả', slug: 'all' }]);
  const [activePost, setActivePost] = useState(null);
  const [activeCategory, setActiveCategory] = useState('all');
  const [searchQuery, setSearchQuery] = useState('');

  useEffect(() => {
    listPostsPublic().then(setAllPosts).catch(() => setAllPosts([]));
    listCategoriesPublic()
      .then((cats) => setCategories([{ id: 'All', name: 'Tất cả', slug: 'all' }, ...cats]))
      .catch(() => {});
  }, []);

  useEffect(() => {
    if (!initialSlug) return;
    getPostBySlugPublic(initialSlug).then(setActivePost).catch(() => setActivePost(null));
  }, [initialSlug]);

  const openPost = (post) => {
    setActivePost(post);
    window.history.pushState(null, '', `/blog/${post.slug}`);
    window.scrollTo(0, 0);
  };

  const backToList = () => {
    setActivePost(null);
    window.history.pushState(null, '', '/blog');
    window.scrollTo(0, 0);
  };

  const filteredPosts = allPosts.filter((post) => {
    const matchesCategory = activeCategory === 'all' || post.categoryName === categories.find((c) => c.slug === activeCategory)?.name;
    const matchesSearch = post.title.toLowerCase().includes(searchQuery.toLowerCase())
      || post.excerpt.toLowerCase().includes(searchQuery.toLowerCase());
    return matchesCategory && matchesSearch;
  });

  return (
    <div className="py-12 md:py-20 animate-fade-in text-left">
      <div className="container mx-auto px-6 max-w-[1232px]">
        {activePost ? (
          <article className="max-w-3xl mx-auto bg-white rounded-[40px] border border-slate-100 p-6 md:p-12 shadow-sm">
            <button
              onClick={backToList}
              className="flex items-center gap-2 text-sm font-bold text-primary hover:underline mb-8"
            >
              <ArrowLeft className="h-4 w-4" />
              Quay lại danh sách bài viết
            </button>

            <span className="text-xs font-bold text-primary bg-primary/10 px-4 py-1.5 rounded-full uppercase tracking-wider">
              {activePost.categoryName}
            </span>

            <h1 className="text-3xl md:text-4xl lg:text-5xl font-extrabold text-slate-800 mt-4 leading-tight text-left">
              {activePost.title}
            </h1>

            <div className="flex flex-wrap items-center gap-4 text-xs text-slate-400 font-semibold mt-6 pb-6 border-b border-slate-100">
              <span className="flex items-center gap-1.5"><User className="h-4.5 w-4.5" />{activePost.author}</span>
              <span className="flex items-center gap-1.5"><Calendar className="h-4.5 w-4.5" />{new Date(activePost.publishedAt).toLocaleDateString('vi-VN')}</span>
              <span className="flex items-center gap-1.5"><Clock className="h-4.5 w-4.5" />{Math.max(1, Math.round(activePost.content.split(/\s+/).length / 200))} phút đọc</span>
            </div>

            <div className="my-8 rounded-3xl overflow-hidden aspect-video bg-slate-100 border border-slate-100">
              <img
                src={activePost.imageUrl}
                alt={activePost.title}
                className="w-full h-full object-cover"
              />
            </div>

            <div
              className="prose max-w-none text-slate-600 space-y-6"
              dangerouslySetInnerHTML={{ __html: activePost.content }}
            />

            <div className="border-t border-slate-100 mt-12 pt-6 flex items-center justify-between text-sm text-slate-400">
              <span>Được biên soạn bởi SIMDULICH.VN</span>
              <button
                onClick={backToList}
                className="font-bold text-primary hover:underline"
              >
                Trở về
              </button>
            </div>
          </article>
        ) : (
          <div>
            <div className="text-center max-w-3xl mx-auto mb-12">
              <span className="text-xs font-bold text-primary bg-primary/10 px-4 py-1.5 rounded-full uppercase tracking-wider">
                Tin Tức & Blog
              </span>
              <h1 className="text-4xl md:text-5xl font-extrabold text-slate-800 mt-4 leading-tight">
                Cẩm nang kết nối di động
              </h1>
              <p className="text-slate-500 mt-4 text-base md:text-lg">
                Tìm kiếm kinh nghiệm du lịch nước ngoài, danh sách máy tương thích eSIM, và hướng dẫn cài đặt mạng nhanh nhất.
              </p>
            </div>

            <div className="flex flex-col md:flex-row items-center justify-between gap-6 mb-12 border-b border-slate-100 pb-8">
              <div className="flex flex-wrap items-center gap-2.5">
                {categories.map((cat) => (
                  <button
                    key={cat.slug}
                    onClick={() => setActiveCategory(cat.slug)}
                    className={`px-5 py-2.5 rounded-full text-sm font-bold transition-all duration-200 ${
                      activeCategory === cat.slug
                        ? 'bg-primary text-white shadow-sm'
                        : 'bg-white border border-slate-100 hover:bg-slate-50 text-slate-600'
                    }`}
                  >
                    {cat.name}
                  </button>
                ))}
              </div>

              <div className="relative w-full md:w-80">
                <input
                  type="text"
                  placeholder="Tìm kiếm bài viết..."
                  value={searchQuery}
                  onChange={(e) => setSearchQuery(e.target.value)}
                  className="w-full bg-white border border-slate-200 focus:border-primary focus:ring-1 focus:ring-primary rounded-full py-2.5 pl-10 pr-4 text-sm outline-none transition-all"
                />
                <Search className="absolute left-3.5 top-3.5 h-4.5 w-4.5 text-slate-400" />
              </div>
            </div>

            {filteredPosts.length > 0 ? (
              <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                {filteredPosts.map((post) => (
                  <article
                    key={post.id}
                    className="bg-white rounded-3xl overflow-hidden border border-slate-100 shadow-sm hover:shadow-xl hover:border-primary/10 transition-all duration-300 group flex flex-col h-full text-left"
                  >
                    <div className="relative overflow-hidden aspect-video bg-slate-100 cursor-pointer" onClick={() => openPost(post)}>
                      <img
                        src={post.imageUrl}
                        alt={post.title}
                        className="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"
                      />
                      <span className="absolute top-4 left-4 bg-white/90 backdrop-blur-md text-primary font-bold text-[11px] px-3 py-1 rounded-full uppercase tracking-wider shadow-sm">
                        {post.categoryName}
                      </span>
                    </div>

                    <div className="p-6 flex flex-col justify-between flex-1">
                      <div>
                        <h3
                          onClick={() => openPost(post)}
                          className="text-lg font-bold text-slate-800 group-hover:text-primary transition-colors leading-snug line-clamp-2 cursor-pointer"
                        >
                          {post.title}
                        </h3>
                        <p className="text-sm text-slate-500 mt-3 leading-relaxed line-clamp-3">
                          {post.excerpt}
                        </p>
                      </div>

                      <div className="mt-6 pt-4 border-t border-slate-50 flex items-center justify-between">
                        <span className="text-xs text-slate-400 font-semibold">{new Date(post.publishedAt).toLocaleDateString('vi-VN')}</span>
                        <button
                          onClick={() => openPost(post)}
                          className="text-xs text-primary font-bold hover:underline flex items-center gap-1"
                        >
                          Đọc tiếp <ChevronRight className="h-3.5 w-3.5" />
                        </button>
                      </div>
                    </div>
                  </article>
                ))}
              </div>
            ) : (
              <div className="text-center py-16 text-slate-400">
                Không tìm thấy bài viết nào phù hợp với yêu cầu tìm kiếm của bạn.
              </div>
            )}
          </div>
        )}
      </div>
    </div>
  );
}
```

Note: `readTime` is now computed inline (`Math.round(word count / 200)`, ~200 words/phút — a
common reading-speed estimate) instead of being a stored field, matching the spec's decision.

- [ ] **Step 9: Run test, confirm it passes**

Run: `npm test -- src/user/pages/__tests__/BlogPage.test.jsx`
Expected: PASS (3 tests).

- [ ] **Step 10: Add `/blog` and `/blog/:slug` deep-link support in `App.jsx`**

In `Simdulich/src/App.jsx`, update `resolvePageFromUrl()` to also recognize blog paths, and thread
the extracted slug down to `BlogPage`. Change the function to return an object instead of a bare
string (update its one caller accordingly):

```js
function resolvePageFromUrl() {
  const path = window.location.pathname.replace(/\/+$/, '');
  const hash = window.location.hash;
  if (path === '/admin' || path === '/admin-ops' || hash === '#admin' || hash === '#/admin' || hash === '#admin-ops') {
    return { page: 'admin-ops' };
  }
  if (path === '/blog') {
    return { page: 'blog' };
  }
  const blogMatch = path.match(/^\/blog\/([^/]+)$/);
  if (blogMatch) {
    return { page: 'blog', slug: blogMatch[1] };
  }
  return null;
}
```

Update the two places that call `resolvePageFromUrl()` and use its result:

```js
  const [currentPage, setCurrentPage] = useState(() => resolvePageFromUrl()?.page ?? 'home');
  const [blogSlug, setBlogSlug] = useState(() => resolvePageFromUrl()?.slug ?? null);
```

```js
  useEffect(() => {
    const openFromUrl = () => {
      const resolved = resolvePageFromUrl();
      if (resolved) {
        setCurrentPage(resolved.page);
        setBlogSlug(resolved.slug ?? null);
      }
    };
    window.addEventListener('hashchange', openFromUrl);
    window.addEventListener('popstate', openFromUrl);
    window.addEventListener('pageshow', openFromUrl);
    return () => {
      window.removeEventListener('hashchange', openFromUrl);
      window.removeEventListener('popstate', openFromUrl);
      window.removeEventListener('pageshow', openFromUrl);
    };
  }, []);
```

Find where `<BlogPage />` is currently rendered (inside the page-switch logic further down in this
same file, alongside `<AboutPage />`/`<ContactPage />` etc.) and pass the slug down:

```jsx
<BlogPage initialSlug={currentPage === 'blog' ? blogSlug : null} />
```

- [ ] **Step 11: Remove the now-unused `blogPosts` field**

In `Simdulich/src/data/defaultSiteContent.js`, delete the entire `blogPosts: [...]` array (the
block you read in full during planning/Task 1 — from the `blogPosts: [` line through its closing
`]`). Confirm no remaining references to `content.blogPosts` exist anywhere in `src/` after this
(the only two call sites were `BlogPage.jsx` and `Blog.jsx`, both already rewritten in this task).

- [ ] **Step 12: Run the full suite, then commit**

Run: `npm test`
Expected: PASS.

```bash
git add src/user/pages/BlogPage.jsx src/user/pages/__tests__/BlogPage.test.jsx src/user/components/Blog.jsx src/user/components/__tests__/Blog.test.jsx src/App.jsx src/data/defaultSiteContent.js
git commit -m "feat: rewire public Blog pages to the real API and add slug-based deep links"
```

---

### Task 8: Manual verification against the real Supabase project + real frontend

**Files:** none (verification only).

**Interfaces:**
- Consumes: the running `simDulichNew` dev server and `Simdulich`'s Blog pages (public + admin).
- Produces: nothing.

- [ ] **Step 1: Apply the new migration**

In the Supabase Dashboard SQL Editor, run `supabase/migrations/0010_blog.sql`.

- [ ] **Step 2: Start both dev servers**

`simDulichNew`: `npm run dev`. `Simdulich`: `npm run dev`.

- [ ] **Step 3: Verify public reads carry the 6 seeded posts**

`GET /api/blog/posts` should return 6 posts, newest `publishedAt` first. `GET
/api/blog/posts/cach-cai-dat-kich-hoat-esim-iphone-android` should return that post's full
`content`. `GET /api/blog/categories` should return the 3 categories.

- [ ] **Step 4: Verify the admin CRUD flow end-to-end in the browser**

Log in as admin, go to "Bài viết Blog" — confirm the 6 seeded posts show with the right category
and "Đã đăng" status. Click "Viết bài mới", fill in a title (confirm slug auto-fills), pick a
category, write some content in the rich-text editor, click "Lưu nháp" — confirm it appears in the
list with "Nháp" status. Edit that same draft and click "Đăng bài" — confirm status flips to "Đã
đăng". Go to "Danh mục Blog", create a new category, confirm it appears; try deleting a category
that has posts — confirm it's rejected with a clear message; delete the category with no posts —
confirm it succeeds.

- [ ] **Step 5: Verify the public site**

Open the homepage — confirm the "Tin tức & Kinh nghiệm nổi bật" section shows real posts. Go to
`/blog` (or click "Xem tất cả bài viết") — confirm all published posts show, filtering by category
and search both work. Click into a post — confirm the URL changes to `/blog/<slug>`, and pressing
F5 on that URL still shows the same post (not the list, not the homepage). Click "Quay lại danh
sách bài viết" — confirm the URL goes back to `/blog`. Confirm the new draft-only post from Step 4
does NOT appear anywhere on the public site until you published it.

- [ ] **Step 6: Report results**

Summarize pass/fail for each step back to the user. Fix any failing step by reading the relevant
route/lib/component file and re-running its automated test before re-verifying manually.

---

## Self-Review

**Spec coverage:**
- Schema + RLS + seed of the 6 old posts — Task 1. ✓
- Public read (no auth) for categories/posts, never exposing drafts — Tasks 2, 3. ✓
- Admin CRUD for categories and posts, `requireRole(['admin','staff'])` — Tasks 2, 3. ✓
- 0-indexed admin pagination (`number` field), no pagination on public list — Task 3 (`listPostsAdmin`
  vs `listPostsPublic`). ✓
- Draft/published workflow, `published_at` set only on first publish — Task 3 (`updatePostAdmin`'s
  existing-row-fetch logic). ✓
- 3 admin pages (categories, posts list, post editor with TipTap) — Tasks 5, 6. ✓
- Slug auto-generation from title/name, Vietnamese-diacritics-aware — Task 5 (`slugify`), reused by
  Task 6. ✓
- No image upload, just a URL field — Tasks 3, 6 (`imageUrl` plain string throughout). ✓
- Public rewiring (`BlogPage.jsx`, `Blog.jsx`) off `SiteContentContext` onto the real API, plus
  `/blog/:slug` deep-linking in `App.jsx` — Task 7. ✓
- Computed (not stored) read time — Task 7 (`BlogPage.jsx`'s inline word-count estimate). ✓
- Manual verification — Task 8. ✓

**Placeholder scan:** No TBD/TODO markers. Every step has runnable code or an exact command with
expected output. The one spot that could look like a placeholder — Task 3's two-attempt explanation
of `updatePostAdmin` — is deliberate: it shows the naive version first specifically to explain why
it's wrong, then gives the actual implementation to use, and says so explicitly.

**Type consistency:** `lib/blogCategories.js`'s and `lib/blogPosts.js`'s `{data, error}` /
`{deleted, error}` return shapes (Tasks 2, 3) are consumed identically by their respective route
files. `slugify(text)` (Task 5) is imported with the same name/signature by Task 6's
`BlogPostEditorPage.jsx`. `blogApi.js`'s function names (Task 4) exactly match what Tasks 5, 6, and
7 import. The public post shape (`categoryId, categoryName, title, slug, excerpt, imageUrl, author,
publishedAt`, plus `content` on the detail endpoint) is identical across `lib/blogPosts.js`'s
`mapPostSummary`/`mapPostDetail`, the route JSON responses, and every frontend consumer
(`BlogPostsPage`, `BlogPage`, `Blog.jsx`) — no task silently renames a field.
